# PRD: EnterTicker

> 이 문서는 기존 기획 문서(`PRD_스윙트레이딩_의사결정보조.md`, 삭제됨)를 잇는 것이 아니라,
> **2026-07-24 시점 실제 코드(`webapp/`)를 직접 읽고 역으로 작성한** "as-built" PRD입니다.
> 즉 "무엇을 만들고 싶었는가"가 아니라 "지금 실제로 무엇이 동작하는가"를 기준으로 썼습니다.
> 앞으로 이 문서를 하나씩 리뷰하며 방향을 다시 잡아나갈 예정입니다.

---

## 0. 제품 한 줄 정의

**나스닥 종목 티커(또는 기업명)를 입력하면, 매수 관점과 매도 관점의 근거를 검색 결과처럼
즉시 나란히 보여주는 웹앱.** 원한다면 "정밀 검토"라는 별도 모드로 들어가 반대 관점을
먼저 강제로 확인하고, 손절/익절가를 산출받고, 매매일지에 기록까지 남기는 8단계 절차를
거칠 수 있다 — 하지만 이건 선택 사항이지 기본 경로가 아니다.

---

## 1. 실제 핵심 루프 (Core Loop)

```
검색창에 티커/기업명 입력
        ↓
Conflict Board — 매수 관점 vs 매도 관점을 2단 컬럼으로 즉시 병치
        ↓ (여기서 끝내도 됨 — 매매일지에는 안 남음)
        ↓ (원한다면)
🔍 정밀 검토 시작 → 포지션 선택 → 반대관점 강제확인 → 지지관점 →
   Conflict Board(재확인) → 손절/익절 산출+확인 → 결정메모(선택) →
   최종확인 → 매매일지 기록
```

**옛 기획과 달라진 가장 중요한 지점:** 원래 구상은 "반대 관점을 먼저 강제로 보여주는 것"
자체가 기본 경험이었다. 지금 실제 구현은 그 반대다 — 기본 경험은 구글 검색결과 화면처럼
빠르고 대칭적인 비교이고, 강제 마찰(반대관점 확인 체크박스, 손절/익절 근거 확인 체크박스)은
"정밀 검토"라는 별도 방으로 들어가야만 작동한다. 사이드바 검색창은 모든 화면에서 항상
떠 있어 언제든 다른 종목으로 즉시 전환 가능하다.

---

## 2. 화면이 지금 실제로 하는 일 (원칙, 사후 정리)

코드에 남은 주석·설계 흔적을 보면 아래 원칙들이 실제로 관철되어 있다:

- **비집계(No Aggregation)**: 어디에도 종합 점수·게이지가 없다. 기술적/이벤트·유동성/
  밸류에이션/정성적 근거가 항상 카테고리별로 나뉘어 병치된다 (`app.py`의
  `collect_bull_bear_lines()`, `render_side()`).
- **방향 색상 미사용**: `evidence()` 헬퍼가 "색상으로 유불리를 암시하지 않는다"는 주석과
  함께 중립적으로 텍스트만 표시. 매수=파랑/매도=주황 컬럼 헤더 정도만 시각적 구분.
- **자동산출 + 확인 강제 (정밀 검토 한정)**: 손절/익절은 지표로 자동 계산되어 입력창에
  채워지지만, "산출 근거 펼쳐보기" expander를 최소 1회 열람하고 체크박스를 눌러야 다음
  단계로 넘어간다. 반대관점 단계도 동일한 패턴(체크박스 없이는 버튼 비활성화).
- **자금관리는 다루지 않음**: 손절/익절가는 자동 산출하지만, 매수 수량·포지션 사이징·계좌
  자본 배분은 어디에도 없다. Step5 화면에 "매수 수량·자금 배분은 이 도구가 다루지
  않습니다"라는 문구가 명시적으로 박혀 있다.
- **정성적 근거는 근사치임을 반복 고지**: 뉴스 톤 분류 옆에 "⚠️ 키워드 매칭 기반 근사치,
  정밀 감성분석 아님" 문구가 Step2/3/Analyst News/Company Events 페이지 전부에 반복 출력.

반면 옛 문서에 있던 "결정 이유 메모는 빈칸이면 진행 불가"는 **더 이상 사실이 아니다** —
현재 Step6은 선택 입력이고 빈칸으로도 다음 단계로 넘어간다. 또한 "면책 고지 상시 노출",
"데이터 신선도 n분 전 타임스탬프 표시"는 코드 전체를 grep해도 어디에도 없다 — 실제로는
구현되지 않은 요구사항이다.

---

## 3. 대상 시장

나스닥/NYSE/AMEX 등 미국 상장 주식. `resolve_ticker()`가 순수 대문자 티커(`^[A-Z]{1,5}(\.[A-Z]{1,3})?$`)는
그대로 쓰고, 그 외 입력은 한글이면 번역 후 Yahoo Finance 검색 API로 미국 거래소
(NMS/NYQ/NGM/NCM/ASE/PCX/BATS/PNK) 종목을 우선 매칭한다.

---

## 4. 목표 / 비목표 (실제 구현 기준)

**지금 실제로 하는 것**
- 티커든 한글/영문 기업명이든 한 번에 검색해서 종목 데이터 전체 수집
- 매수/매도 근거를 카테고리별(기술적·이벤트유동성·밸류에이션·정성적)로 나눠 즉시 병치
- 손절/익절가를 여러 후보 중 보수적 기준(MAX/MIN)으로 자동 산출, 근거 라벨 표시
- 동일 산업/섹터 peer 대비 Forward PER 위치 + 재무 건전성(EV/Revenue·유동성·부채·런웨이) 병치
- 기관/내부자 소유구조, 공매도 비율, 유동주식비율, 액티브/패시브 펀드 비중, 내부자 매매 방향성
- 애널리스트 투자의견 집계 + 관련 뉴스, M&A/경영진교체/신규계약 이벤트 뉴스 분류
- **관계도(Relationship Map, 2026-07-25 신규)**: M&A·신규계약/파트너십 뉴스에서 "이미 아는
  회사"(나스닥·뉴욕 대형주 정적 목록 + peer 리스트 + 세션 검색 이력)가 상대로 등장하면
  원형 관계 그래프로 시각화, 유형/진행상태/근거수준 표시
- 옵션 미결제약정/거래량 스냅샷(만기별 요약 + 행사가별 상세)
- 정밀 검토를 완주하면 CSV 매매일지에 기록

**하지 않는 것**
- 매수/매도를 대신 결정해주지 않음, 단일 점수로 합산하지 않음
- 자동 주문 실행 없음
- 매수 수량·포지션 사이징·계좌 리스크 계산 없음 (전면 삭제됨)
- 개인화 투자자문 없음
- 재무제표 자체는 여전히 yfinance `info` 필드에 의존 — SEC EDGAR는 관계도의 공시 검색
  용도로만 씀(6.8절), 재무 데이터 소스로는 안 씀
- 면책 고지 문구 없음 — **로드맵상 공백으로 다뤄야 할 항목** (10번 참조)

---

## 5. 화면 구성 (Information Architecture)

`webapp/app.py`가 `st.session_state.step` 값으로 단일 파일 내 상태머신을 구성하고,
`webapp/pages/`의 8개 파일이 Streamlit 멀티페이지로 상세 데이터를 나눠 보여준다.
`.streamlit/config.toml`에서 `showSidebarNavigation=false`로 자동 네비를 끄고,
`lib/search.py`의 `render_sidebar()`(검색창+링크)를 모든 페이지가 공통으로 렌더링한다.

### 5.1 랜딩 화면 (`step == "search"`)
- `layout="centered"`. 화면 중앙에 워드마크 "Enter**Ticker**"(두 번째 단어만 블루 `#2f6fed`).
- 검색창+"Enter" 버튼이 하나의 둥근 필(pill) 안에 병합된 폼. Enter 키 제출 지원.
- placeholder: "티커 또는 기업명을 입력하세요 (예: NVDA, Nvidia, 엔비디아)"

### 5.2 Conflict Board (`step == "compare"`)
- `layout="wide"`로 전환. 워드마크 "Conflict **Board**".
- 2단 컬럼: 🔵 매수 관점 / 🟠 매도 관점. 각 컬럼 안에 4개 서브섹션
  (기술적 근거/이벤트·유동성/밸류에이션/정성적 근거), 카테고리당 5줄 미리보기 +
  "더보기(N개)" expander.
- 실적일이 영업일 기준 0~10일 이내로 임박했을 때만, 유동주식비율 30% 미만일 때만
  방향 무관 caption 노출 (평소엔 생략 — "급한 것만" 원칙).
- 하단 버튼 "🔍 정밀 검토 시작 (반대 관점부터 단계별로)" → intent 화면으로.

### 5.3 사이드바 (전 페이지 공통, 티커 조회 후에만 노출)
- 미니 검색 폼(다른 종목으로 즉시 전환, 전환 시 이전 종목의 intent/memo/stop/take_profit/
  entry_price는 초기화됨)
- 8개 서브페이지로 가는 네비게이션 링크 (아이콘+영문 라벨)

### 5.4 서브페이지 8종 (`pages/`, 전부 `require_analysis()`로 미조회 시 메인으로 유도)

| # | 파일 | 화면 | 핵심 내용 |
|---|---|---|---|
| 1 | `1_차트.py` | Price Chart | 일봉(1M/3M/6M/1Y/전체) 또는 분봉(1분/5분/15분/30분/1시간, 상호 배타적 라디오) 캔들차트 + MA20/MA60 + 거래량. 분봉은 KST로 변환 표시, Yahoo 제약(1분=최근7일 등) 고지 |
| 2 | `2_Peer_목록.py` | Peer List | 전체 peer(Tier1+Tier2) 테이블 — tier·판정근거(동일산업/동일섹터·시총/니치키워드)·티커·기업명·Forward PE·EV/Revenue·당좌비율·런웨이 |
| 3 | `3_섹터_Peer_비교.py` | Peer Compare | 대상 Forward PE, 니치 키워드 그룹(자동판별), Tier1 평균/중앙값(n<3이면 "표본부족" 표기), 재무건전성 expander(EV/Revenue·당좌비율·런웨이·PBR·D/E·ROE, 레버리지 인지형 해석) |
| 4 | `4_소유구조.py` | Ownership Map | 종합 요약표(기관/내부자보유율·공매도비율·유동주식비율·펀드Passive:Active·내부자매매방향성) + 개별 상세 + 내부자 거래 원본(주식보상 제외) 테이블 |
| 5 | `5_애널리스트_뉴스.py` | Analyst News | 최근 60일 뉴스 중 등급/목표가 키워드 매칭분(최대 8건), 한글 번역, 매칭 키워드 표시 |
| 6 | `6_기업_이벤트_뉴스.py` | Company Events | M&A·경영진교체·신규계약 키워드 매칭(최대 10건), 카테고리·매칭키워드 표시 |
| 7 | `7_옵션_데이터.py` | Options Data | 향후 6개월 내 만기별 콜/풋 OI·거래량 합계 + 풋/콜비율, 만기 선택 시 행사가별 상세(OI/거래량/IV), 해석 가이드 expander |
| 8 | `8_관계도.py` | Relationship Map | (2026-07-25 신규) M&A/신규계약·파트너십 뉴스 기반 관계 그래프. 허브(현재 티커) 중심 원형 배치, 유형별 색상(M&A/공급계약/합작투자/라이선싱/전략적제휴), 철회·무산 상태는 점선, 호버에 상대 회사명·유형·최신 상태·근거수준·헤드라인 표시 |

각 페이지 상단에 `render_wordmark()`로 통일된 2단어 타이포그래피, 하단/본문에 관련 용어
`lib/glossary.py` 접이식 설명 배치.

### 5.5 정밀 검토 (선택 기능, `step`이 `"intent"` 또는 정수 2~8)

```
intent  : 포지션 선택(매수검토/매도검토), 선택 없으면 에러
step 2  : 반대 관점 강제 노출 — 기술적/이벤트유동성/밸류에이션/정성적 근거 중
          "의도의 반대 방향"만 필터링해서 표시. 체크박스 확인 전엔 다음 버튼 비활성화.
step 3  : 지지 관점 — 같은 구조, 강제 체크박스 없음(그냥 확인용)
step 4  : Conflict Board 재노출 — 양쪽 다 근거가 있으면 "보수적으로 접근" 경고
step 5  : 손절/익절 자동산출 — 진입가 입력(매수 검토 시) 또는 현재가 고정(매도 검토 시,
          "청산 판단"이라는 문구로 신규 공매도와 구분). 후보값 3~4개 expander로 펼쳐보고
          체크박스 확인해야 다음 단계 진행. 값은 직접 수정 가능.
step 6  : 결정 이유 메모 — **선택 입력**, 빈칸 허용
step 7  : 최종 확인 — 티커/의도/손절/익절/메모 요약. "그대로 진행"(=journal 기록) 또는
          "값 수정 후 진행"(step5로 복귀)
step 8  : 완료 — trade_journal.csv 전체를 데이터프레임으로 표시, "새 종목 분석하기" 버튼
```

모든 정밀 검토 화면 상단에 `"🔍 정밀 검토 — EnterTicker"` 헤더 + 진행률 바
(`정밀 검토 {n}/7 — {라벨}`, 라벨: 반대관점/지지관점/Conflict/손절익절/결정메모/최종확인/기록완료).
어느 화면에서든 "🔄 처음부터 다시(전체 초기화)" 버튼으로 session_state 전체 리셋 가능.

---

## 6. 기능 요구사항 상세

### 6.1 티커/기업명 검색 (`lib/search.py`, `lib/data.py::resolve_ticker`)
- 순수 대문자 1~5글자(+선택적 `.XXX` 접미사)는 티커로 즉시 사용
- 한글 포함 시 `deep_translator`로 영문 변환 후 Yahoo Finance 검색 API 호출
  (`yahoo_symbol_search`, 한글 쿼리 직접 지원 안 함 — 400 에러 확인됨)
- `EQUITY` 타입 중 미국 거래소(NMS/NYQ/NGM/NCM/ASE/PCX/BATS/PNK) 우선 채택, 없으면
  아무 EQUITY, 그것도 없으면 원본을 대문자로 강제 변환해 최후 시도
- 검색으로 찾은 경우 `st.toast()`로 "'입력값' → 티커 (매칭기업명)" 알림

### 6.2 데이터 수집 및 가드 (`fetch_and_store_ticker`)
- FinanceDataReader로 2024-01-01부터 가격 이력 조회, 실패 시 에러 후 중단
- **60거래일 미만이면 중단** — MA60 등 롤링 지표가 NaN이 되는 것을 막는 가드
  (2026-07-24 추가, QNT 등 최근 IPO 종목으로 실증 확인됨)
- 지표 계산(`compute_indicators`) → yfinance info/calendar → QQQ 200일선 대비 시장국면 →
  peer 분류 → 재무건전성 → 소유구조 → 펀드 active/passive → 내부자거래 → 신호분류 →
  뉴스(56일) 톤분류(관련성 게이트 통과분만) → 애널리스트 트렌드 → 넓은기간(60일)
  뉴스로 애널리스트관련/기업이벤트 필터링
- 새 검색 시 이전 종목의 `intent/memo/stop/take_profit/entry_price`를 session_state에서 제거

### 6.3 기술적 지표 (`lib/indicators.py`)
- RSI(14), MA20/MA60(골든/데드크로스는 "직전 봉 대비 방금 교차"만 감지, 정배열/역배열과
  별개 판정), 볼린저(20, ±2σ, 상단/하단 근접만 텍스트 판정, %B 수치 자체는 미구현),
  ATR(14), MACD(EMA12/26, 시그널 EMA9, 히스토그램 부호전환으로 크로스 판정)
- 각 지표를 bullish/bearish/neutral로 분류해 `signals` 리스트로 반환

### 6.4 손절/익절 자동 산출 (`lib/risk.py::compute_stop_take_profit`)
- 손절 후보: ATR×2, 20일 스윙로우−ATR×0.3, 볼린저 하단, (MA20<진입가일 때만) MA20
  → `entry - ATR×1`보다 낮은 후보만 유효 취급, 그중 **가장 높은 값(MAX, 가장 보수적)** 채택
- 익절 후보(50% 물량 기준): R:R 1.5, 직전 60일 전고점, 볼린저 상단
  → **가장 낮은 값(MIN, 가장 보수적)** 채택. 나머지 50%는 목표가 없이 트레일링(설명 문구만,
  실제 트레일링 로직 코드는 없음 — UI 문구로만 존재)
- 매수 수량/포지션 사이징 계산은 없음

### 6.5 섹터 Peer 비교 (`lib/peers.py::classify_peers`)
- Finnhub `company-peers`로 후보 확보
- Tier 판정 우선순위: **동일 산업(industryKey/industry) 일치 → 동일 섹터+시총 0.1~10배
  밴드 → 니치 키워드(rare_earth/lithium/uranium/copper 사전, business summary 매칭)**
  순으로 라벨링(`tier_basis`), 판정 자체는 OR — 셋 중 하나만 맞아도 Tier1
- Tier1 표본만으로 Forward PE 평균/중앙값 계산, n<3이면 "표본 부족" 표기
- 재무건전성 보완: EV/Revenue, PBR, ROE(부채비율과 함께 해석), 유동/당좌비율, D/E,
  현금런웨이(보유현금÷|연간FCF|×12, FCF≥0이면 "흑자전환")

### 6.6 소유구조 (`lib/ownership.py`)
- 기관/내부자 보유율, 공매도 비율, 유동주식비율(float/shares outstanding)
- 펀드 단위 액티브/패시브: 펀드명에 `index/etf/spdr/ishares/s&p/russell/total market` 등
  키워드 포함 시 Passive, 아니면 "Active(추정)" — 법인 단위 매핑은 없음(키워드 무효화 알려짐)
- 내부자 매매 방향성: `Text`에 "Purchase"/"Sale" 포함된 건만 방향 집계(주식보상/Award 제외,
  옵션행사 등은 "기타"로만 카운트), 매수주식수 vs 매도주식수 비교로 순매수/순매도 판정

### 6.7 뉴스/정성적 근거 (`lib/qualitative.py`)
- **관련성 게이트(`is_relevant`)**: Finnhub `related` 필드에 티커가 있거나, 헤드라인+요약에
  티커/기업명 식별토큰(접미사 제거, 3자 이상)이 단어경계로 등장해야 통과. 둘 다 없으면
  게이트 미적용(하위호환)
- 단어경계 정규식 매칭(`_matched_keywords`)으로 "cut→execu**tive**" 같은 부분문자열 오탐 방지
- 긍정 키워드 중 "raises concern(s)" 등 부정 맥락 문구가 붙으면 긍정 카운트에서 제외
- 경영진교체는 직함(ceo/cfo/president/chairman 등) **AND** 변경동사(steps down/resigns/
  appoints 등) 둘 다 있어야 매칭 — 단순 CEO 언급만으로는 오탐 안 되게 분리
- 애널리스트 관련 뉴스는 "initiates coverage" 등 구체적 문구로 좁혀 필터 (과거 "coverage"
  단독 키워드가 무관 기사 오매칭시킨 이력 있음)

### 6.8 관계도 (`pages/8_관계도.py`, 2026-07-25 신규, 2026-07-25 SEC 공시 소스 추가)

**엣지 소스 두 갈래 — 뉴스(즉시 로딩) + SEC 공시(관계도 페이지 방문 시 지연 로딩)**

**(A) 뉴스 기반** (`lib/qualitative.py::match_counterparties`, 검색 시 즉시 계산)
- 스코프를 의도적으로 좁힘: 엣지는 **M&A + 신규계약/파트너십 뉴스만**(경영진교체·경쟁관계 제외,
  경쟁관계는 이미 Peer Compare가 다룸)
- 매칭 로직: 후보 회사명에서 접미사(inc/corp/holdings 등) + 업종 일반명사(technology/
  semiconductor/systems/solutions 등 — "ON Semiconductor"/"TE Connectivity"처럼 접미사만
  떼면 업종 일반 단어 하나만 남아 아무 기사에나 오매칭될 뻔한 사례 발견 후 추가)를 제거한
  토큰이 **전부(ALL)** 헤드라인에 있어야 매칭(단일 토큰 ANY 매칭은 "Micron Technology"/
  "Marvell Technology"의 "technology"처럼 흔한 단어 하나로 무관한 회사가 오매칭된 실제 사례
  때문에 폐기됨), **또는** 티커 심볼이 원문 그대로(대소문자 구분) 단어경계로 등장하면 매칭
  (3자 미만 티커는 "ON"처럼 흔한 단어와 겹쳐 제외)
- "신규 계약/파트너십" 버킷은 매칭 키워드로 공급계약/합작투자/라이선싱/전략적제휴로 세분화,
  진행상태(철회·무산/발표·추진/체결·진행)는 헤드라인 키워드 근사 판정

**(B) SEC 공시 기반** (`lib/sec_filings.py::find_filing_relationships`, 2026-07-25 신규)
- **도입 이유(실측)**: 뉴스 전용일 때 티커당 엣지가 2~3개뿐이라 "너무 적다"는 피드백 →
  SEC EDGAR Full-Text Search(무료, API 키 불필요)로 10-K/10-Q/8-K 공시까지 확장. NVDA로
  실측 시 뉴스 2~3개 → 뉴스+공시 합쳐 15개 상대 회사·52개 엣지로 증가(예: CoreWeave/MSFT/
  Oracle/Alphabet — peer도 아니고 뉴스에도 안 잡히던 실제 고객사·파트너 관계).
- **매칭 방식이 뉴스와 다름**: 일반적 관계 문구("supply agreement", "strategic partnership")를
  그대로 검색하면 거의 안 걸림을 실측 확인(NVDA 최근 2년 "supply agreement" 검색 0건 — 공시는
  격식체라 뉴스와 다른 어휘를 씀). 그래서 **"상대 회사 이름 자체가 대상 기업 공시 본문에
  등장하는지"**로 검색(`ciks` 파라미터로 대상 기업 공시만 필터링). 법인 접미사만 제거하고
  업종 일반명사는 남김(뉴스 매칭과 반대 — "Taiwan Semiconductor"처럼 그 단어가 회사명의
  핵심인 경우가 많아서)
- 검색 대상: `lib/known_companies.py::STATIC_KNOWN_COMPANIES`(~100개) + peer 리스트, 티커당
  최대 ~110개 후보를 `ThreadPoolExecutor(max_workers=5)`로 병렬 조회(SEC 권장 10req/s 이내),
  결과는 (대상 CIK, 검색구문) 단위로 `st.cache_data(ttl=86400)` 캐시
- **정밀 관계 유형·방향성은 판단 안 함** — 히트가 있으면 무조건 "공시상 언급"/상태 "공시 확인"
  으로만 표시(응답에 스니펫이 없어 문맥 파악 불가 — 경쟁사 비교·소송 등 부정적 맥락일 수도
  있음을 캡션에 명시하고, 원문 링크를 눌러 직접 확인하도록 유도). 근거수준 "공시 자료(SEC
  EDGAR, 공식 문서)"로 뉴스("뉴스 보도 기반, 공식 확인 아님")보다 신뢰도 높음을 구분 표시.
- **알려진 함정(실제로 겪음)**: SEC가 요구하는 User-Agent 형식이 까다로워서, 괄호·URL이
  들어간 문자열("EnterTicker (github.com/...)")은 403으로 막히고 "앱이름 이메일형식 문자열"
  ("EnterTicker research contact@example.com")은 통과함 — 실측으로 맞는 형식을 확인 후 반영.

**병합 및 지연 로딩** (`pages/8_관계도.py`)
- 뉴스 엣지(검색 시 즉시)는 `st.session_state.relationship_edges`, 공시 엣지는 관계도
  페이지 첫 방문 시에만 계산되어 `filing_edges`에 저장(Ownership Map의 지연로딩과 동일
  패턴) — 진행률 바(`st.progress`)로 "SEC 공시자료에서 관계 확인 중... (n/110)" 표시,
  같은 세션 재방문은 즉시 로딩. 두 소스는 스키마(`relationship_type`/`status`/
  `evidence_level`/`headline`/`url`/`datetime`)가 같아 `render_relationship_graph_figure()`
  하나가 그대로 병합 렌더링(한 회사가 뉴스+공시 둘 다에서 잡히면 노드 하나에 두 유형·두
  근거수준이 함께 표시됨 — Tesla 실측 사례로 확인).
- 시각화: `networkx` 등 그래프 레이아웃 라이브러리 없이 `math.cos/sin` 원형 배치(노드 수가
  항상 작아 강제 배치 알고리즘 불필요). 철회·무산 상태는 점선, 정상은 실선. 범례는 실제
  등장한 유형만 동적 표시(뉴스 5종 + "공시상 언급" 회색)
- **하지 않는 것**: 관계 방향성(누가 인수/피인수·공급/수요인지), 공시 본문 파싱을 통한 정밀
  문맥 분류, 지분보유/자회사 구조 파악, 회사 웹사이트 스크래핑(범용화 불가로 스코프 제외,
  `relationship_map_design_handoff.md` 참조), Finnhub Supply Chain API(프리미엄 등급, 비용 발생)

### 6.9 애널리스트 의견 (Finnhub recommendation trends)
- 최신 기간 strongBuy+buy vs strongSell+sell 비교, buy가 sell의 1.5배 초과면 bullish,
  반대면 bearish, 아니면 neutral

### 6.10 옵션 데이터 (`lib/data.py`)
- 만기 목록 중 오늘부터 185일 이내만 대상
- 만기별 콜/풋 OI·거래량 합계, 풋/콜 OI 비율(0으로 나누기 방지)
- 특정 만기 선택 시 행사가별 콜/풋 OI·거래량·IV 테이블

### 6.11 매매일지 (`lib/journal.py`)
- `webapp/trade_journal.csv`에 append-only 기록: timestamp/ticker/intent/entry_price/
  stop/take_profit/memo
- **정밀 검토 Step7에서 "그대로 진행"을 눌러야만 기록됨** — Conflict Board만 보고
  끝내는 사용자의 행동은 전혀 기록되지 않음
- Streamlit Cloud 재배포 시 파일 시스템이 초기화되어 **기록이 사라짐** (영속화 미해결)

### 6.12 번역 (`lib/translate.py`)
- `deep_translator.GoogleTranslator` — 뉴스 헤드라인 한글 번역(`to_korean`), 한글 검색어
  영문 변환(`to_english`). 둘 다 24시간 캐시, 실패 시 원문 그대로 폴백. 비공식 API라
  향후 차단 가능성 있음.

---

## 7. 데이터 파이프라인 (실제 흐름)

```
[검색 입력] → resolve_ticker() → ticker
                                    │
        ┌───────────────────────────┼───────────────────────────┐
        ▼                           ▼                           ▼
FinanceDataReader           yfinance(.info/.calendar/      Finnhub(peers/
(가격 이력)                  mutualfund_holders/            recommendation/
   │                        insider_transactions/           company-news)
   │                        options)
   ▼
compute_indicators() → classify_indicator_signals()
        │
        ├─ classify_peers() (Finnhub 후보 + yfinance industry/sector/summary)
        ├─ get_financial_health() (yfinance info)
        ├─ get_ownership_summary()/get_fund_level_active_passive()/
        │  get_recent_insider_transactions() (yfinance)
        └─ classify_news_tone()/filter_analyst_related_news()/
           filter_corporate_event_news() (Finnhub news + 관련성 게이트)
        │
        ▼
   st.session_state에 전부 저장 → Conflict Board / 서브페이지 / 정밀검토가 공유
```

캐싱(`st.cache_data` TTL): 가격 1시간, 분봉 5분, yfinance info/calendar/holders 1시간,
Finnhub peers/recommendation 1시간, Finnhub news 30분, 옵션 30분, 번역 24시간.

---

## 8. 데이터 소스 (실제 사용 중)

| 카테고리 | 소스 | 비고 |
|---|---|---|
| 가격/차트(일봉) | FinanceDataReader | 2024-01-01~ |
| 가격(분봉) | yfinance | 인터벌별 조회가능 기간 제한, KST 변환 |
| 펀더멘털/소유구조/섹터/옵션 | yfinance | 비공식 스크래핑, 데이터 누락 가능성 |
| Peer 목록/뉴스/애널리스트 의견 | Finnhub | 무료 티어, API 키 필요(`.env` 또는 Streamlit Secrets) |
| 기업명→티커 검색 | Yahoo Finance 비공식 검색 API | 한글 쿼리 미지원, 번역 후 호출 |
| 뉴스 번역 | deep-translator(Google Translate 비공식) | |
| 관계도 "이미 아는 회사" 목록 | `lib/known_companies.py` 정적 하드코딩(~100개) | 새 API 아님, 코드에 박아둔 스냅샷 — 10번 참조 |
| **SEC EDGAR** | **관계도 공시 검색용으로 사용** (`lib/sec_filings.py`, 2026-07-25 추가) | Full-Text Search API, 무료·키 불필요. 재무제표 소스로는 안 씀(여전히 yfinance) |

---

## 9. 비기능 요구사항 — 실제 vs 옛 기획 대조

| 항목 | 옛 기획 | 실제 구현 |
|---|---|---|
| 데이터 신선도 타임스탬프 표시 | 요구사항으로 명시 | ❌ 없음 (캐시 TTL은 있으나 화면 미노출) |
| 면책 고지 상시 노출 | 요구사항으로 명시 | ❌ 어디에도 없음(전체 grep 0건) |
| 소스 실패 시 부분 실패 허용 | 요구사항으로 명시 | ✅ 대부분 함수가 `try/except`로 `None`/`{}`/`[]` 폴백 |
| 반대관점 확인 없이 진행 불가 | 핵심 원칙 | ✅ (정밀검토 Step2 한정), ❌ (Conflict Board 기본 화면은 처음부터 양쪽 다 보임) |
| 결정 메모 필수 입력 | 필수로 명시 | ❌ 선택 입력으로 완화됨 |

---

## 10. 알려진 한계 / 리스크 (실측 기준)

- **면책 고지·데이터 신선도 미구현**: 9번 표 참조 — 실사용 전 반드시 채워야 할 공백.
- **매매일지 미영속화**: Streamlit Cloud 재배포 시 CSV 초기화.
- **정밀 검토를 완주해야만 데이터가 남음**: Conflict Board만 보고 이탈하는 사용자 행동은
  전혀 로깅되지 않아, "반대 근거를 보고 포지션을 철회했는가" 같은 핵심 가설을 지금 구조로는
  측정 불가.
- **뉴스 관련성 게이트의 한계**: Finnhub `related` 필드가 여러 티커를 묶어 놓은 기사(예:
  업종 전반을 다루는 기사)는 게이트를 통과해 엉뚱한 종목의 "매도 관점"에 노출될 수 있음
  (NVDA 검색 시 Tesla/Rivian/Lucid 기사가 노출된 사례 실측 확인, 2026-07-24).
  단어경계 매칭·직함+동사 이중조건으로 상당수 오탐은 제거됐지만 완전하지 않음.
  LLM 기반 분류로 정밀도를 높이는 방향은 사용자가 "돈 안 드는 방향"을 선택해 보류 중.
  Finnhub Forward PER 데이터 품질: yfinance 비공식 스크래핑이라 소형주 등에서 누락/부정확 가능.
- **액티브/패시브 펀드 분류**: 펀드 단위 키워드 매칭만 있고, 법인 단위(BlackRock Inc. 등)
  대형 운용사 매핑은 없음 — 회사명에 전략이 안 드러나 매칭 실패.
- **볼린저 %B 미구현**: 상단/하단 근접 여부 텍스트 판정만 있고 정규화 수치·밴드폭 계산 없음.
- **락업 해제일 데이터 없음**: 공식 API에 필드가 없어 SEC 서류 직접 확인이 필요한
  리서치 갭으로 남음.
- **전체화면 사이드바 중복 버그**: 사용자가 Peer Compare 페이지 전체화면 전환 시 사이드바가
  2벌 보인다고 리포트했으나, 로컬 프리뷰에서 재현 실패(원인 미확정, 2026-07-24 기준 미해결).
- **트레일링 손절(나머지 50% 물량)은 UI 문구로만 존재**: 실제로 추격손절 값을 계산/추적하는
  코드는 없음 — Step5/7 화면에 설명 텍스트만 있고 별도 로직 없음.
- **관계도 커버리지는 "이미 아는 회사" 목록 크기에 완전히 종속됨** (2026-07-25 신규 기능):
  `lib/known_companies.py`의 정적 목록은 특정 시점 스냅샷이라 상장폐지·사명변경·신규 대형주
  편입 등으로 시간이 지나면 어긋날 수 있음 — 주기적 수동 갱신 필요. 이 목록 + peer 리스트 +
  세션 검색 이력 밖의 회사는 실제로 뉴스에 관계가 언급돼도 그래프에 아예 나타나지 않음(재현율
  손실을 정확도·무비용과 맞바꾼 의도된 설계).
- **관계도는 방향성·관계의 정밀한 유형을 모름**: "누가 인수하고 누가 인수당했는지",
  "누가 공급하고 누가 받는지"를 구분하지 못함 — 헤드라인에 두 회사가 같이 등장한다는 것만
  안다. 진행상태(철회/발표/체결) 판정도 헤드라인 문구 기반 근사치라 발표 단계 M&A와 완료된
  M&A를 완벽히 구분하지 못하는 경우가 있을 수 있음.
- **관계도 매칭의 잔여 오탐 위험**: 회사명에서 업종 일반명사를 뺀 뒤 남는 토큰이 여전히
  흔한 단어인 회사(예: 데이터에는 없지만 향후 정적 목록에 추가될 수 있는 이름들)는 원리상
  오탐 가능성이 남아있음 — "Block, Inc."(→"block")·"Booking Holdings"(→"booking")·"Unity
  Software"(→"unity")는 이런 이유로 정적 목록에서 아예 제외함(실증 전 사전 차단).

---

## 11. 스코프 밖 (아직 손대지 않은 것)

- SEC EDGAR를 재무제표 소스로 쓰는 것(관계도 공시 검색 용도로만 씀, 6.8절 참조 — 재무제표는
  여전히 yfinance)
- 백테스트 게이트(손절/익절 공식을 과거 데이터로 검증)
- 매매일지 클라우드 영속화(Google Sheets 등)
- 애널리스트 공신력 가중치(과거 적중률 기반)
- 면책 고지, 데이터 신선도 표시(9번 참조 — 우선순위 높은 공백)
- 회사 웹사이트 스크래핑(관계도 소스로 검토했으나 범용화 불가로 제외, 6.8절 참조)
- 관계도의 공시 문맥 정밀 분류(경쟁사 비교 vs 실제 파트너십 구분 등) — 현재는 "언급됨"까지만

---

## 12. 관련 문서 (참고용, 세부 계산식·실증 히스토리)

- `부록_손절익절_계산스펙.md` — 손절/익절 계산식·파라미터, 삭제된 포지션사이징 기능의 역사
- `데이터소스_조사_나스닥.md` — 데이터 소스 선정 조사
- `세션_인계.md` — 세션 간 인계 로그(진행 상황, 다음 할 일)
- `relationship_map_design_handoff.md` — 관계도 기능의 "정공법" 설계안(SEC EDGAR/Finnhub
  Supply Chain API/LLM 추출/방향성·상태 모델 등). 지금 구현은 이 문서와 비교해 스코프를
  크게 좁힌 무료 버전 — 방향성 없음, 새 API 없음, 6.8절 참조. 비용을 들여 "정공법"으로
  갈지는 아직 미정.
