# Changelog

## 2026-07-28
- **Financial Statements 페이지 신규 추가**(`pages/2_재무제표.py`) — 매출·마진·순이익·CapEx·
  OCF·FCF 등 핵심 재무 지표(yfinance 연간 재무제표 기반)와, 있으면 그 항목에 대한 "경영진
  코멘트"(실적발표 8-K 인용, 없으면 발표성 뉴스로 폴백)를 함께 보여준다. 항목당 인용 개수를
  강제하지 않고 실제로 찾은 만큼만 — 대부분의 회사·분기는 매치가 없는 게 정상이며 그럴 땐
  "공개된 설명 없음"으로 정직하게 비워둔다
- 실측 검증: 10-K MD&A는 CapEx가 큰 회사(NVIDIA/Micron)조차 구체적 이유가 없었고, 뉴스는
  단순 키워드로는 "AI 캐펙스 버블" 류 오피니언 칼럼만 걸렸다 — 그래서 1순위를 실적발표
  8-K(NVIDIA의 CFO Commentary처럼 항목별 서술이 있는 회사는 여기서 잡힘), 2순위를 좁게
  필터링한 뉴스(금액/시설명 + 발표동사 조합, 오피니언 문구 제외)로 설계
- 8-K 첨부문서의 재무제표 표(HTML table)가 태그 제거 후 숫자 나열로 평평해지면서 문장으로
  오인되는 문제(Micron/Salesforce 실측으로 재현) — 숫자 토큰 밀도로 표 오탐을 걸러내고,
  비GAAP 용어 정의 문단("The Company defines...")도 실제 설명이 아니라서 함께 제외
- `lib/sec_filings.py`의 스트리밍 fetch·HTML 정리·문장 경계 자르기 로직을 `lib/filing_text.py`로
  추출(관계도·재무제표 코멘트 양쪽에서 재사용, 동작 변화 없음) — `test_sec_snippet_streaming.py`로
  회귀 없음 확인

## 2026-07-27 (3차)
- 관계도 그래프의 **조작을 전부 비활성화**(휠 확대·툴바·드래그·더블클릭 확대·축 확대) —
  가격 차트와 같은 plotly config를 공유하고 있어서, 그래프 위에 커서를 두고 휠을 굴리면
  페이지가 안 내려가고 그래프가 확대돼 버렸다. 근거 표가 그래프 아래에 있어 스크롤이
  정상 경로인데 그게 막히던 문제. hover는 그대로 남긴다(`staticPlot`은 hover까지 죽여서 안 씀)
- 관계도 hover 말풍선 **줄바꿈 추가** — plotly는 hover 텍스트를 자동 줄바꿈하지 않아서, 긴
  공시 발췌문 한 줄이 그림 영역보다 넓어지고 화면 밖으로 잘려 나갔다. 단어 경계로 끊고
  (한글은 2폭으로 계산) 공백 없는 초장문 토큰은 강제 분할
- 관계도 hover의 발췌문 길이를 220자 → 150자로, 표시 건수를 3건 → 2건으로 축소
  (전체 발췌문과 원문 링크는 아래 표가 이미 다 보여준다)

## 2026-07-27 (2차)
- 관계도 노드 안에 **회사 로고 표시** — Finnhub `company-profile2`의 `logo` 필드 사용
  (이미 쓰던 무료 티어 키, 비용 없음). 그래프에 실제로 그려지는 상위 10개 + 허브에
  대해서만 병렬 조회하고 캐시. 로고가 없는 회사(소형주에 흔함)는 로고 도입 전과
  똑같이 빈 원으로 폴백되므로 최악의 경우에도 기존 대비 손해가 없다
- 로고를 **원형으로 잘라 노드 원을 채우도록** 변경(`lib/logos.py` 신규) — plotly는 이미지
  원형 클리핑을 지원하지 않아, 받아온 로고를 가운데 정사각형으로 자르고 원형 알파 마스크를
  씌운 PNG로 다시 만들어 data URI로 넘긴다. 검증에서 알파>0 픽셀 면적비가 0.785(=π/4)로
  나와 정확히 원인 것을 확인. 가로로 긴 워드마크형 로고(비율 1.6 초과)는 자르면 글자가
  날아가므로 자르지 않고 내접시킨다. PIL이 못 읽는 형식(SVG 등)은 원본 URL로 내접 폴백
- Pillow를 `requirements.txt`에 명시(streamlit이 이미 의존하지만 직접 쓰게 됐으므로)
- 위 작업에 따라 노드 원을 픽셀 단위 marker에서 **데이터 좌표 shape**으로 변경 — layout
  image는 데이터 좌표라, 원이 픽셀이면 창 크기가 바뀔 때 로고가 원 밖으로 삐져나온다
- 관계도 엣지 선을 노드 중심이 아니라 **원 테두리에서 끊도록** 변경(중심까지 그으면 선이
  원 안을 가로질러 로고 위에 겹친다 — 원이 비어 있을 땐 안 보이던 문제)
- 뉴스 헤드라인 번역을 병렬 선반영으로 전환 — 예전엔 Conflict Board가 그려지는 도중에
  헤드라인 30건을 하나씩 순차 번역해서, 데이터 수집 스피너가 끝난 뒤 아무 안내 없이
  화면이 멈춘 것처럼 보였다. 이제 수집 단계에서 미리 동시에 번역한다
  (deep_translator의 `translate_batch()`는 내부적으로 for문이라 요청 수가 안 줄어드는 것을
  소스로 확인 — 요청 수를 줄이는 대신 동시에 보내는 방향을 택함)
- Analyst News / Company Events 페이지에도 같은 선반영 적용
- 표시 개수 상수를 `lib/config.py`로 모음(번역 선반영 개수와 화면 표시 개수가 어긋나면
  캐시 미스가 나서 다시 느려지므로 한 곳에서 관리)
- 관계도 SEC 공시 문맥 추출을 스트리밍으로 전환 — 회사명을 찾는 즉시 연결을 끊는다.
  가짜 10-K 검증에서 문서의 2.7%만 읽고 중단(실제 10-K 본문은 약 1.9MB)
- 관계도 스니펫 캐시를 정제 텍스트 전체(수 MB)에서 결과 스니펫(수백 바이트)만 담도록 축소.
  워커 스레드에서 `st.cache_data`를 호출하던 것도 함께 해소
- 관계도 문맥 추출 대상을 상위 20개 회사 → 그래프에 실제로 표시되는 10개로 축소
- 관계도에 **근거 원문 표 2종 추가** — 상대기업별 요약표(그래프에 안 그려진 회사까지 전부)와
  근거 전체 표(날짜·소스·발췌문·원문 링크). 예전엔 이 정보가 그래프 hover 안에만 있어서
  원문으로 바로 갈 수 없었고 터치 환경에서는 hover 자체가 안 떴다
- 관계도 그래프 가독성 개선 — 12시 방향에서 시계방향 배치, 노드가 6개 이상이면 반지름을
  엇갈리게 줘서 라벨 겹침 방지(노드 간 최소거리 0.618 → 0.729), 유형별 색을 노드에도 적용,
  라벨을 허브 반대쪽에 배치, 허브 강조. 노드 크기는 근거 수와 무관하게 균일 유지(원칙 B)
- 관계도 그래프에 상위 10개만 그린다는 사실과 잘린 개수를 화면에 명시
- 관계도 주의사항 문구를 한 줄 요약 + 접이식 상세로 분리(6줄짜리 한 덩어리라 안 읽히던 것)
- `.gitattributes` 추가 — 내용 변화가 없는데 줄바꿈만 CRLF로 바뀌어 6개 파일이 통째로
  수정된 것처럼 잡히던 문제 정리
- 분리 후 남아있던 옛 `PRD_EnterTicker.md` 삭제(참조하던 문서들의 링크도 정리)

## 2026-07-27
- 정밀 검토(8단계 강제 흐름)와 매매일지 기능을 라이브 앱에서 제거
- 손절/익절 자동산출 로직도 함께 비활성화(코드는 아카이브에 보존)
- Peer List 페이지를 Peer Compare 페이지에 통합(사이드바 8개→7개 항목)
- Peer Compare에 Current Ratio, Operating Margin, Gross Margin, EV/EBITDA, 매출성장률
  (YoY) 지표 추가
- Peer Compare에 재무 특징 서술형 코멘트 추가(규칙 기반, 새로운 판단 없음)
- Ownership Map에 "기관+내부자 합계" 항목 추가(두 수치가 100%로 안 합산됨을 명시)
- 사이드바 세션 초기화 링크를 "시작화면으로"로 개명해 검색창 위에 작게 재배치
- 모든 서브페이지 헤더(타이틀·티커·뒤로가기)를 중앙 정렬로 통일
- Options Data: 콜 OI 합계가 0인 만기에서 발생하던 크래시 수정
- Options Data: 행사가별 상세 테이블 정렬 기준을 미결제약정(OI)에서 거래량(volume)으로 변경
- Price Chart: 일봉 기간 라디오 버튼의 기본 선택 표시가 실제 표시 중인 기간(6개월)과
  불일치하던 문제 수정
- 사이드바에 현재가+당일 변동률 표시 추가(상승 빨강/하락 파랑, 티커+가격 한 줄)
- 사이드바 세션 초기화가 서브페이지에서 눌렀을 때 중간 화면을 한 번 더 거치던 문제 수정
  (`st.switch_page`로 바로 이동)
- Relationship Map: SEC 공시 검색을 양방향으로 확장(대상 종목 공시 + 상대 회사 공시 모두
  검색) — 정방향만으론 못 잡던 상대방(예: 자기 공시에만 언급을 크게 실은 소형주) 포착
- Relationship Map: 정적 "이미 아는 회사" 목록에 우주/방산·희토류/critical minerals·
  암호화폐 채굴 카테고리 추가(~100개 → ~120개)
- Relationship Map: 그래프 위에 상대기업 수·뉴스 근거 수·SEC 공시 근거 수를 분리 표시

## 2026-07-25
- Relationship Map 화면 신규 추가(뉴스 기반 기업 관계 그래프)
- SEC EDGAR 공시 검색을 Relationship Map의 두 번째 데이터 소스로 추가(무료, 키 불필요)
- SEC 공시 검색 결과에 실제 계약 문맥 스니펫 표시 추가

## 2026-07-24
- 최근 IPO 등 가격 이력이 60거래일 미만인 종목의 검색을 막는 가드 추가
- 뉴스 관련성 게이트 보완(단어경계 매칭, 경영진교체 이중조건, 애널리스트 뉴스 필터 정교화)
- Peer 페이지 순서 조정, Ownership Map 정리, Options Data 문구 개선
