# Relationship Map (관계 지도) — 설계 인계 문서

> EnterTicker 신규 페이지 기획안. Claude Code 인계용.
> 작성일: 2026-07-25

## 1. 개요

기업 간 실제 관계(M&A, 지분/자회사, 공급망, 제휴)를 근거 기반으로 탐색하는 그래프 페이지.
목표는 "예쁜 마인드맵"이 아니라 **누가 누구와, 어떤 관계로, 언제부터, 어떤 근거로 연결되는지**를 사용자가 3초 안에 파악하고 1클릭으로 근거를 확인하게 하는 것.

- 메뉴명(한글): 관계 지도
- 영문 보조명: Relationship Map
- 설명 문구: "기업 간 인수·지분·공급망·제휴 관계를 근거와 신뢰도 기준으로 탐색합니다."
- 대상 범위: **나스닥 상장사 한정** (글로벌/한국 시장 커버리지는 스코프 아님)

## 2. 관계 유형 (v1 스코프)

| 유형 | 방향 | 설명 |
|---|---|---|
| M&A (인수/피인수) | 인수사 → 대상 | 완료/발표/철회 상태 구분 필요 |
| 지분보유/자회사 | 보유사 → 피투자사 | 실질 지배 또는 경제적 노출 |
| 공급 | 공급사 → 고객 | 매출·생산 의존 관계 |
| 제조/위탁생산 | 브랜드·설계사 → 제조사 | 생산 병목 파악용 |
| 전략적 제휴 | 회사 ↔ 회사 | 공동 개발·판매·기술 제휴 |

- **경쟁 관계는 제외** — 기존 Peer Compare 기능과 역할이 중복되므로 스코프에서 뺀다.
- **중요도 점수(수치화/랭킹)는 v1에서 완전히 제외**. 이유: 정량 데이터(매출 비중 등)가 대부분 익명화되어 있어 신뢰할 수 없는 임의 가중치가 될 위험이 큼. 정보 전달을 우선하고, 순위 기능은 실제 데이터 확보량이 확인된 이후 재검토.

## 3. 데이터 모델

노드보다 엣지(관계) 중심 스키마. 관계마다 근거와 신뢰 수준을 반드시 포함.

```python
relationship = {
    "id": "rel_nvda_tsmc_foundry_2025",
    "source_company_id": "ticker:NVDA",
    "target_company_id": "ticker:TSM",
    "relationship_type": "contract_manufacturing",  # m&a / equity / supply / manufacturing / partnership
    "direction": "source_to_target",  # or "bidirectional"
    "status": "active",  # active / announced / terminated / rumored
    "evidence_level": "confirmed",  # confirmed / reported / inferred / rumor
    "start_date": "2025-01-01",
    "end_date": None,
    "evidence": [
        {
            "source_type": "sec_filing",  # sec_filing / company_press / news / inferred
            "url": "...",
            "published_at": "2025-02-26",
            "excerpt": "원문에서 추출한 짧은 근거 요약",
            "retrieved_at": "2026-07-24"
        }
    ],
    "last_verified_at": "2026-07-24"
}
```

- `evidence_level`은 신뢰도(사실 확인 여부)이며 중요도 점수와는 다른 개념. 이건 유지.
- 근거(evidence) 없는 관계는 화면에 노출하지 않는다.
- rumor/watchlist 상태는 기본 화면에서 숨김 처리.

## 4. UI/UX 설계 원칙

### 4.1 정보 노출 3단계 (Progressive Disclosure)

| 단계 | 노출 정보 |
|---|---|
| 기본 화면 | 그래프 구조만 (색, 화살표, 실선/점선) — 텍스트 라벨 없음 |
| 호버 | 한 줄 요약 (예: "SK hynix → NVIDIA · HBM 공급 · 확인됨") |
| 클릭 | 우측 패널에 전체 근거, 날짜, 출처 링크, 관계 설명 |

### 4.2 링크(엣지) 스타일

- 색상 = 관계 유형 (4~5개 이내 팔레트로 제한)
- 화살표 = 방향
- 실선/점선 = 확인됨/추정됨
- 굵기 = **균일 또는 유형별 고정 2단계** (중요도 점수 기반 아님 — v1에서 점수 제외 결정에 따름)
- 라벨은 상시 노출 금지. 겹침·혼잡 방지를 위해 호버/클릭 시에만 표시.

### 4.3 노드 스타일

- 원(circle) + 티커 텍스트, 로고 있으면 로고 우선·티커 폴백
- 원 크기 = 시가총액 기준, 로그 스케일 적용 (최소/최대 크기 고정)
- 테두리 색 = 산업/카테고리
- 중심(검색 대상) 기업은 가장 크게 + 테두리 두껍게

### 4.4 거리(distance)에 대한 원칙

**노드 간 거리를 중요도 표현에 사용하지 않는다.** Force-directed 레이아웃에서 거리는 이미 "직접 연결 여부/그래프상 단계"를 나타내는 데 쓰이므로, 여기에 중요도까지 얹으면 사용자가 오독한다. 거리는 구조(그래프 topology)에게 맡기고, 강조하고 싶은 값은 색/크기/굵기 등 검증된 채널로 표현한다.

### 4.5 복잡도 관리 (Hairball/Starburst 방지)

- 기본 진입 시 핵심 노드 8~15개, 링크 15~25개 이하만 표시
- 관계 유형 필터 (M&A만/공급망만/제휴만 보기)
- "확인된 관계만 보기" 토글 기본 ON
- 2-hop 확장은 사용자가 명시적으로 "더 보기"를 눌렀을 때만
- 관계가 많은 허브 기업은 하위 그룹으로 묶어(clustering) 접어두기

## 5. 데이터 커버리지 및 표기 규칙

나스닥 한정이라도 시총 구간별로 데이터 확보량이 다르므로, **데이터 부재를 숨기지 않고 명시적으로 표기**한다.

| 시총 구간 | 기준(예시, 파일럿 후 조정) | 표기 |
|---|---|---|
| Large cap | 100억 달러 이상 | 기본 표시, 뱃지 없음 |
| Mid cap | 20억~100억 달러 | 필요시 "일부 데이터 제한적" |
| Small/Micro cap | 20억 달러 미만 | 검색 결과·그래프 진입 시 안내 배너 노출 |

### 표기 위치
1. **검색 결과 카드**: 🟢 관계 데이터 충분 / 🟡 관계 데이터 제한적일 수 있음
2. **그래프 진입 배너**: "이 기업은 대형주 대비 공개된 관계 데이터가 적습니다. 공급망·제휴 정보는 SEC 공시, 뉴스 보도 기준으로 확인 가능한 범위만 표시됩니다."
3. **관계 0건인 경우**: 빈 그래프 대신 안내 카드로 대체 (버그처럼 보이지 않게)

주의: 뱃지를 모든 종목에 남발하지 말 것. 실제 데이터가 부족한 하위 구간에만 표기.

## 6. 데이터 소스 및 수집 전략

| 우선순위 | 소스 | 용도 | 비고 |
|---|---|---|---|
| 1차 | SEC EDGAR (submissions API, Full Text Search, XBRL) | M&A, 지분/자회사, 고객 집중도 | 인증키 불필요, 고객명 대부분 익명화(Customer A/B) |
| 1차 | Finnhub Supply Chain Relationships API | 공급망 관계 | 프리미엄 등급 필요, 기존 앱이 Finnhub 이미 사용 중 |
| 2차 | Finnhub 뉴스 `related` 필드 | 관계 후보, co-occurrence 강도 근사 | LLM 추출 결과는 반드시 `evidence_level: reported/inferred`로 저장 |
| 2차 | OpenCorporates API | 법인 지배관계 보강 | 글로벌 공급망 소스 아님, 지분 구조 검증용으로만 |
| MVP 초기 | 수동 시드 데이터 | 검증 정확도 확보 | CSV/SQLite, 10~20개 종목, 관계 50~100건 |

### LLM 추출 시 원칙
- 원문 인용(evidence_quote) 없이 결론만 저장 금지
- 구조화된 JSON 출력만 받고, 항상 `reported` 또는 `inferred`로 태깅
- confirmed로 승격은 별도 검증 프로세스 필요 (v1에서는 수동 검토 큐로 처리)

## 7. 구현 파일 구조 제안

기존 lib/pages 구조를 유지하며 신규 모듈만 추가.

```text
lib/
  relationships.py         # 스키마, CRUD, 그래프 데이터 확장
  relationship_sources.py  # EDGAR·뉴스·법인 데이터 수집
  relationship_extract.py  # LLM/규칙 기반 후보 추출
  relationship_ui.py       # 그래프·상세 패널 렌더링

data/
  relationships_seed.csv   # MVP 수동 검증 데이터
  relationship_cache.db    # 캐시, 검토 상태, 근거 저장

pages/
  8_Relationship_Map.py
```

- `lib/ownership.py`는 그대로 소유구조 전용 유지, `relationships.py`가 지분 관계를 별도 타입으로 통합.
- `relationship_score.py` (중요도 계산) 는 v1에서 생성하지 않음 — 추후 필요 시 추가.

## 8. MVP 로드맵

| 주차 | 작업 | 참고 |
|---|---|---|
| 1주차 | 수동 시드 그래프 구축 (종목 10~20개, 관계 50~100건) | M&A/지분/제휴/공급망만 |
| 2주차 | Plotly 기반 그래프 UI, 필터, 호버 툴팁, 우측 상세 패널 | 기존 앱 Plotly 의존성 재사용 |
| 3주차 | EDGAR·뉴스 기반 관계 후보 추출 파이프라인 | entity resolution(회사명 중의성) 이슈로 예상보다 오래 걸릴 수 있음, 일정 2배 여유 확보 권장 |
| 4주차 | 데이터 커버리지 표기, 빈 상태(empty state) UI, 검토 큐 프로세스 | |

## 9. 알려진 리스크 / 열린 이슈

- **Entity resolution 문제**: 뉴스·공시 텍스트에서 회사명을 정확히 매칭하는 로직이 필요 (예: "Apple"이 회사인지 아닌지 등). 3주차 일정 지연 요인.
- **Confirmed 승격 프로세스 미정**: LLM/규칙 기반으��� 뽑은 reported/inferred 관계를 누가, 어떤 기준으로 confirmed로 올릴지 아직 미정 — v1에서는 수동 검토로 처리하고 이후 자동화 검토.
- **Finnhub Supply Chain API 요금제 미검증**: 실제 응답 스키마와 비용을 계정으로 직접 테스트 필요.
- **중요도 점수 재도입 조건**: (1) 정량 데이터가 실제로 몇 개 관계에서 확보되는지 파일럿으로 확인된 이후, (2) 사용자 피드백에서 "관계가 너무 많아 우선순위를 모르겠다"는 불만이 나온 이후. 그때도 숫자보다 정성적 표시(높음/중간/낮음) 우선 검토.

## 10. v1에서 명시적으로 제외한 것

- 중요도 점수 계산 및 순위 기반 정렬/필터
- 경쟁(Peer) 관계 (기존 Peer Compare 기능과 역할 중복)
- 나스닥 외 시장(한국, 기타 글로벌 거래소)
- 노드 간 거리로 중요도 표현
- 링크 위 상시 텍스트 라벨
- 자동 confirmed 승격 (수동 검토로 대체)
