# EnterTicker — 기능 인수인계 문서

> 이 문서는 **지금 이 순간 코드가 실제로 무엇을 하는지**만 다룬다. 왜 그렇게 만들었는지,
> 언제 무엇을 고쳤는지 같은 변경 이력은 담지 않았다 — 그런 배경이 필요하면
> `docs/CHANGELOG.md`, `docs/AS_BUILT_TECH_SPEC.md`, `세션_인계.md`를 참고할 것.

---

## 1. 한 줄 요약

**EnterTicker** — 나스닥/NYSE/AMEX 상장 미국 주식 티커를 검색하면, 그 종목의 기초 정보와
매수/매도 근거가 되는 데이터를 8개 서브페이지에 나눠 보여주는 Streamlit 웹앱. 점수를 합산해
"사라/팔아라"를 판정하지 않고, 있는 데이터를 병치해서 보여주기만 한다.

- 로컬 코드: `webapp/` (진입점 `app.py`, 공용 로직 `lib/`, 서브페이지 `pages/`)
- GitHub: https://github.com/zzang-pyeong/financial-assistant
- 배포된 앱: https://financial-assistant-2g4f3appzsgmwj7aiwzbdzj.streamlit.app/
- 로컬 실행: `.claude/launch.json`에 등록된 `streamlit run webapp/app.py` (포트 8501)
- 필요한 비밀값: `webapp/.env`(로컬) 또는 Streamlit Cloud Secrets(배포)에 `FINNHUB_API_KEY` 1개

---

## 2. 화면 흐름

```
검색(랜딩) → 회사 소개(기초 정보) → 사이드바에서 서브페이지 8개 자유 이동
```

- 티커 조회 전에는 사이드바가 아예 안 보인다. 조회 성공 시에만 사이드바(세션 초기화 링크 +
  미니 검색창 + 서브페이지 8개 네비게이션)가 나타난다.
- 사이드바 표시 순서는 파일명이 아니라 `webapp/lib/_shared_core/search.py`의
  `_SIDEBAR_PAGES` 리스트가 결정한다.

### 검색

한글/영문 모두 입력 가능(`엔비디아`, `Nvidia`, `NVDA` 전부 인식). 한글이 섞이면 번역 후
Yahoo Finance 검색 API로 티커를 찾는다. 최근 60거래일 가격 데이터가 없는 종목(최근 IPO 등)은
지표 계산이 불가능해 검색 자체를 막는다.

### 회사 소개

검색 직후 뜨는 메인 화면(`webapp/app.py`). 로고 포함 티커 헤더 + 시가총액·직원 수·
배당수익률(배당성향 툴팁)·상장일 메트릭 카드 4개 + 본사·CEO 한 줄. 여기서 끝 — 매수/매도
근거는 병치된 한 화면이 아니라 아래 8개 서브페이지에 나뉘어 있다(기술적 신호는 Price Chart,
밸류에이션·재무건전성은 Peer Compare, 애널리스트 의견은 Analyst News, 실적 임박 경고는
Financial Statements, 공매도·저유동 경고는 Ownership Map).

---

## 3. 서브페이지 8개 (현재 기능)

| # | 파일 | 화면 | 지금 보여주는 것 |
|---|---|---|---|
| 1 | `1_차트.py` | Price Chart | 상단에 **기술적 신호 4개**(RSI/이동평균/볼린저밴드/MACD, bullish·bearish·neutral 색상 아이콘) + 일봉(1개월/3개월/6개월/1년/전체) 또는 분봉(1분~1시간, 상호배타) 캔들차트 + MA20/MA60 + 거래량. 분봉은 KST 변환 표시 |
| 2 | `2_재무제표.py` | Financial Statements | **실적발표 임박 경고**(영업일 기준 D-10 이내만 노출) + **분기/연간 토글**(기본 분기) + **조회 기간 선택**(과거 분기·연도 검색 가능) + 핵심 지표 카드(매출·매출총이익률·영업이익률·순이익·CapEx·OCF·FCF·CapEx/매출, 전기 대비 증감 %·%p 표시, 색상 없음) + 매출/CapEx 추이 막대그래프 + **경영진 코멘트**(실적발표 8-K 원문 인용, 없으면 뉴스로 보충, 그마저 없으면 "공개된 설명 없음") |
| 3 | `3_섹터_Peer_비교.py` | Peer Compare | 대상 종목 Forward PE, 자동판별된 니치 키워드 그룹, Tier1(진짜 동종업계) 평균/중앙값, 재무건전성 expander(EV/Revenue·EV/EBITDA·유동/당좌비율·현금런웨이·PBR·D/E·ROE·영업이익률·매출총이익률·매출성장률 + 서술형 요약), 하단에 전체 Peer 목록(Tier1+Tier2, **시가총액 포함**) 표 — peer 정보는 병렬 조회로 수집 |
| 4 | `4_소유구조.py` | Ownership Map | 종합 요약표(기관/내부자 보유율·둘의 합계와 그 의미·공매도비율·유동주식비율·펀드 Passive:Active 비율·내부자 매매 방향성) + 내부자 거래 원본 테이블(최근 10건) |
| 5 | `5_애널리스트_뉴스.py` | Analyst News | **애널리스트 투자의견 분포**(매수/보유/매도 카운트 + 기준 시점), 목표주가(평균/최고/최저/현재가 대비 상승여력), **최근 1년 상향/하향 등급 변경 이력 표**(날짜·증권사·증권사 규모 티어·등급 변화·당시 주가·목표주가 변화), 최근 60일 애널리스트 관련 뉴스(최대 8건, 한글 번역) |
| 6 | `6_기업_이벤트_뉴스.py` | Company Events | M&A·경영진 교체·신규 계약/파트너십 키워드 매칭 뉴스(최대 10건, 한글 번역, 매칭 분류 표시) |
| 7 | `7_옵션_데이터.py` | Options Data | 향후 6개월 내 만기별 콜/풋 미결제약정(OI)·거래량 합계 + 풋/콜 비율, 특정 만기 선택 시 행사가별 상세(거래량순/행사가순 토글), 지표 해석 가이드 expander |
| 8 | `8_관계도.py` | Connection Map | 뉴스(M&A·계약·제휴) + SEC 공시(공시 내 언급, 대량 지분 보유 13D/13G) 기반 기업 관계 그래프. 관계 유형/기간 필터, 공시 내 언급 노출 토글, KPI(상대기업 수·지분보유 건수·M&A·계약 건수), 상대기업별 요약표(관계유형·방향·상태·지분율·핵심 발췌 원문 인용·원문 링크, 한글 번역 토글), 근거 원문 전체 표 |

각 페이지 상단은 2단어 워드마크 + 로고 포함 티커 헤더 + 뒤로가기로 통일돼 있고, 필요한
곳은 하단에 `lib/glossary.py` 용어 설명이 접이식으로 붙어 있다.

---

## 4. 지금 지켜지고 있는 설계 규칙 (새 기능도 이 틀 안에서 만들 것)

- **비집계 원칙**: 여러 지표를 하나의 점수·등급으로 합산하지 않는다. 병치만 한다.
- **색상으로 유불리를 암시하지 않는다**: 매수=파랑/매도=주황처럼 컬럼 구분에만 색을 쓰고,
  개별 수치나 델타에는 색을 넣지 않는다(예: 재무제표 페이지 증감률은 색 없이 숫자만).
- **모르면 모른다고 표시한다**: 데이터가 없으면 빈 값·"N/A"·"공개된 설명 없음"으로 두고
  추측해서 채우지 않는다. 추정치를 쓸 때는 반드시 "(추정치)"를 표기한다.
- **투자 판단은 사용자 몫**: 앱은 매수/매도를 판정하지 않고 근거만 제공한다.
- 새 LLM/유료 API 도입 없이 무료 데이터 소스(yfinance/Finnhub 무료 티어/SEC EDGAR/
  FinanceDataReader/deep-translator)만 쓴다.

---

## 5. 코드 구조 — `webapp/lib/`

의존관계 기준으로 폴더가 나뉘어 있다(자세한 설명은 `webapp/lib/README.md` 참고).

| 폴더 | 성격 |
|---|---|
| `_shared_core/` | 전 페이지 사이드바 허브(`search.py`)가 의존하거나 2개 이상 페이지가 공유. 여기를 고치면 여러 페이지에 영향 — 영향 범위를 먼저 확인할 것 |
| `_shared_page2_page8_filings/` | SEC 공시 텍스트 검색(`sec_filings.py`, `filing_text.py`) — 재무제표 페이지와 관계도 페이지가 공유 |
| `page2_only_financials/` | 재무제표 페이지 전용(`financials.py`) — 다른 곳에서 안 씀, 단독 수정 안전 |
| `page8_only_relationship/` | 관계도 페이지 전용(`logos.py`, `sectors.py`) — 단독 수정 안전 |
| `_legacy_unused/` | 현재 어떤 페이지도 안 씀(`risk.py`) — 비활성 상태인 `webapp/archive/precision_review.py`에서만 참조 |

`webapp/archive/precision_review.py`는 예전 "정밀 검토" 8단계 흐름 코드를 삭제하지 않고
보존만 해둔 것 — `pages/` 밖에 있어 Streamlit이 자동으로 등록하지 않으므로 지금은 실행되지
않는다.

---

## 6. 데이터 소스 요약

| 데이터 | 출처 | 비고 |
|---|---|---|
| 가격(일봉) | FinanceDataReader | 2024-01-01~ |
| 가격(분봉) | yfinance | 인터벌별 조회가능 기간 제한 |
| 재무제표/펀더멘털/소유구조/옵션 | yfinance | 비공식 스크래핑, 결측 가능 |
| Peer 목록/뉴스/애널리스트 의견/목표주가 | Finnhub | 무료 티어, API 키 필요 |
| 기업명→티커 검색 | Yahoo Finance 비공식 검색 API | 한글 쿼리 미지원(번역 후 호출) |
| 뉴스/헤드라인 한글 번역 | deep-translator(Google 비공식) | 실패 시 원문 폴백 |
| 관계도 "이미 아는 회사" 목록 | `lib/_shared_core/known_companies.py` 정적 하드코딩 | 이 목록 밖 회사는 관계도에 안 잡힘 |
| SEC 공시 검색/재무제표 8-K 인용 | SEC EDGAR Full-Text Search + 개별 공시 원문 | 무료, 키 불필요 |

---

## 7. 지금 알려진 한계 (고치는 김에 참고)

- 데이터 신선도(마지막 갱신 시각) 표시 없음, 투자 조언 아님 고지 어디에도 없음.
- 뉴스 관련성 필터가 완전하지 않아 여러 티커가 묶인 기사가 무관 종목에도 노출될 수 있음.
- 액티브/패시브 펀드 분류는 펀드명 키워드 매칭뿐, 운용사 단위 매핑 없음.
- 관계도는 정적 회사 목록 크기에 커버리지가 종속되고, 관계의 방향성·정밀 유형은 판단하지
  않음(언급 여부만 확인).
- Peer 정보 수집이 순차 루프라 종목당 peer 수만큼 느려짐(병렬화 안 됨).
- 락업 해제일 데이터 없음(공식 API에 필드 자체가 없음).
- `webapp/archive/precision_review.py`는 `lib.journal`(삭제된 모듈)을 여전히 import해서
  재활성화 없이 그대로 두면 깨진 상태 그대로다.

---

## 8. 로컬 개발 팁

- 로컬 서버: `.claude/launch.json`의 `devils-advocate-webapp` 설정으로 실행
  (`streamlit run webapp/app.py --server.port 8501`).
- 코드 수정이 반영 안 되면 서버 재시작 + `webapp/__pycache__` 삭제를 의심할 것.
- 이 프로젝트는 Codex로도 동시에 편집될 수 있다 — 낯선 diff를 발견하면 에러가 없는 한
  건드리지 말고, push 전에는 `git fetch` + `git log main..origin/main`으로 원격에 새 커밋이
  있는지 먼저 확인할 것.
