# 관계도(Relationship Map) — SEC 공시 소스 확장

> 구현 완료. 작성일: 2026-07-25.

## 왜 추가했나

관계도를 뉴스(M&A/파트너십 키워드, 최근 60일)만으로 만들었더니 티커당 엣지가 2~3개뿐이라
"말이 안 된다"는 피드백을 받음. 원인 3가지:

1. 최근 60일 M&A/파트너십 키워드에 걸리는 뉴스 자체가 원래 적음
2. 뉴스에 진짜 관계가 나와도 "이미 아는 회사"(peer+정적목록+검색이력) 밖이면 누락
3. 60일 창이 짧아 오래됐지만 여전히 유효한 관계를 놓침

회사 웹사이트 스크래핑도 대안으로 검토했으나 회사마다 페이지 구조가 달라 범용화 불가 →
제외. 대신 **SEC EDGAR Full-Text Search**(무료, API 키 불필요)를 새 소스로 추가.

## 무엇이 달라졌나

**대상 기업의 10-K/10-Q/8-K 공시 본문에, "이미 아는 회사"(peer+정적목록, 티커당 최대
~110개 후보) 각각의 이름이 등장하는지 검색.** 뉴스 매칭과 달리 관계 유형 문구
("supply agreement" 등)로는 검색하지 않음 — 실측 결과 정확한 문구 매칭은 거의 안 걸림
(NVDA 최근 2년 "supply agreement" 검색 0건, 공시는 격식체라 뉴스와 어휘가 다름). 대신
회사 이름 자체("Taiwan Semiconductor", "Micron" 등)를 검색해 훨씬 높은 적중률을 얻음.

히트가 있으면 정밀한 관계 유형·방향성 판단 없이 **"공시상 언급"**으로만 표시(응답에
스니펫이 없어 문맥 파악 불가 — 경쟁사 비교·소송 등 다른 맥락일 수도 있음을 명시하고
원문 링크로 직접 확인하도록 유도). 근거수준은 "공시 자료(SEC EDGAR, 공식 문서)"로
뉴스("뉴스 보도 기반, 공식 확인 아님")보다 신뢰도 높음을 구분 표시.

## 실측 결과 (NVDA)

| | 뉴스만 | 뉴스 + SEC 공시 |
|---|---|---|
| 상대 회사 수 | 1~2개(AMKR 등) | **15개** |
| 총 엣지 수 | 2~3개 | **52개** |
| 소요 시간(첫 방문) | 즉시 | 약 17.6초(병렬 5개, 이후 세션 내 캐시로 즉시) |

새로 잡힌 회사: Microsoft, Intel, Oracle, **CoreWeave**(peer도 아니고 뉴스에도 안 잡히던
실제 GPU 클라우드 고객사), Arm, Coherent, Alphabet, Tesla, Cisco 등. 특히 CoreWeave 사례가
이번 확장의 핵심 가치를 보여줌 — peer 리스트(경쟁사 분류)로도, 뉴스로도 절대 안 잡혔을
관계가 8-K 실적 발표 보도자료에서 반복적으로 확인됨.

## 아키텍처

```
검색 → NVDA
   ├─ 즉시: 뉴스 기반 엣지 계산 (기존 로직 그대로, lib/qualitative.py)
   └─ 관계도 페이지 방문 시 지연 로딩:
        lib/sec_filings.py::find_filing_relationships()
        1. get_cik("NVDA")  ← company_tickers.json 캐시
        2. known_companies(peer+정적목록, ~110개) 각각에 대해
           ThreadPoolExecutor(max_workers=5)로 병렬 검색
           efts.sec.gov/LATEST/search-index?q="회사명"&ciks={CIK}&forms=10-K,10-Q,8-K
        3. 히트마다 엣지 생성, st.progress로 진행률 표시
        4. (대상 CIK, 검색구문) 단위로 24시간 캐시
   → 두 소스 병합(같은 스키마) → render_relationship_graph_figure() 하나로 렌더링
```

같은 회사가 뉴스와 공시 양쪽에서 잡히면(예: Tesla) 노드 하나에 두 유형·두 근거수준이
함께 표시됨 — 실제로 확인됨.

## 구현 중 발견해서 고친 버그 2가지

1. **User-Agent 403 차단**: SEC가 자동화 요청에 식별 가능한 User-Agent를 요구하는데,
   curl로 검증했던 문자열("EnterTicker research contact@example.com")과 실제 코드에 넣은
   문자열(괄호+GitHub URL 포함)이 달라서 전부 403으로 막혔음. 정확한 형식으로 맞춰서 해결
   — **API를 검증한 정확한 문자열을 코드에 그대로 옮겨야 한다는 교훈.**
2. **근거수준(evidence_level) 하드코딩**: 그래프 hover 텍스트의 "신뢰도" 줄이 뉴스 전용
   문구("뉴스 보도 기반, 공식 확인 아님")로 하드코딩돼 있어서, 공시 기반 노드에도 똑같이
   잘못 표시됐음. 실제 `evidence_level` 필드를 쓰도록 수정 — 뉴스만 있을 때는 이 버그가
   드러나지 않다가, 두 번째 소스를 섞으면서 발견됨.

## 알려진 한계

- 공시 검색 결과에 문맥 스니펫이 없어, 히트가 곧 "긍정적 관계"라는 뜻은 아님(경쟁사 비교,
  소송, 단순 업종 언급일 수도 있음) — 이건 UI 캡션에 명시하고 원문 링크로 유도하는 것 외엔
  근본 해결 안 함(스니펫을 얻으려면 공시 문서 자체를 파싱해야 해서 스코프 밖으로 남김)
- 방향성(누가 인수/피인수, 공급/수요) 여전히 모름
- 검색 대상이 "이미 아는 회사"(peer+정적목록)로 제한 — 이 목록 밖의 실제 관계는 여전히 누락
- 첫 방문 시 약 17초 대기(캐싱으로 재방문은 즉시) — 후보 수를 줄이는 대신 병렬화+진행률
  표시로 체감 대기시간을 낮추는 쪽을 선택함(사용자 확인)

## 변경 파일

- 신규: `webapp/lib/sec_filings.py`
- 수정: `webapp/pages/8_관계도.py`(지연 로딩+병합), `webapp/lib/charts.py`("공시상 언급" 색상 +
  근거수준 표시 버그 수정), 당시 `PRD_EnterTicker.md`(6.8/8/11절 — 이 PRD는 2026-07-27
  `docs/PRD.md`+`docs/AS_BUILT_TECH_SPEC.md`+`docs/CHANGELOG.md`로 분리된 뒤 삭제됨)
