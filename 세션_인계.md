# 세션 인계 문서 — Devil's Advocate / EnterTicker 프로젝트

> 새 대화(컨텍스트)로 넘어갈 때 이 문서 하나만 붙여넣으면 프로젝트 전체 맥락을 파악할 수
> 있도록 작성했습니다. 작성일: 2026-07-24 (직전 세션 기준 대폭 갱신 — 이전 인계 문서를 덮어씀).

---

## 0. ⚠️ 지금 당장 처리해야 할 것

**6-C 코드 변경은 커밋됐지만, 브라우저 실증 검증은 아직 끝나지 않았습니다.**

`4e9fa67` (`Remove unused firm-holder code, fix earnings D-day and old branding, guard short price history`)에
6-C 코드 변경이 커밋되어 있다. 브라우저 실증 검증만 미완료 상태다.

새 세션에서 가장 먼저 할 일:
1. 프로젝트 루트에서 `git status`로 위 상태 재확인
2. **브라우저로 실증 검증부터 마칠 것** — 특히:
   - NVDA/USAR 검색이 정상 동작하는지 (이번 수정에서 `get_firm_level_holders` 체인을
     통째로 삭제했는데, `st.session_state.firm_holders`를 어디서도 안 쓰는지 재확인은
     grep+pyflakes로 완료했지만 실제 실행은 확인 못 함)
   - 가격 이력 60일 미만 종목(최근 상장) 검색 시 "가격 이력이 너무 짧아..." 에러 메시지가
     정상적으로 뜨는지 (신규 추가한 가드, 실행 검증 안 됨)
   - 실적 발표일 D-day 로직 수정(과거 날짜를 "임박"으로 오판하던 버그) 확인
3. 검증 결과를 기록할 것. 6-C 자체는 이미 커밋됐으므로 재커밋하지 않는다.

해당 4개 파일의 변경 내용은 아래 "6-C. 이번 세션 코드 감사 및 정리"에 설명되어 있습니다.

### Codex 후속 작업 기록 (2026-07-24)

- `webapp/.streamlit/config.toml`에 `[theme] base = "light"`를 추가해 앱 테마를 흰색으로 고정했다.
- 로컬 Python 환경에서 Streamlit 실행에 `packaging`이 누락되어 있었으므로
  `webapp/requirements.txt`에 명시했다. `http://localhost:8501/_stcore/health`는 `200 ok`로 확인했다.
- 사용자와 검색 지연 개선 방향을 합의했다. **아직 성능 개선 코드는 작성하지 않았다.**
  1. `fetch_and_store_ticker()` 단계별 시간을 먼저 기록해 실제 병목을 측정한다.
  2. 56일/60일 뉴스 중복 요청은 60일 요청 한 번으로 통합하고, 펀드 보유·내부자 거래는
     소유구조 페이지에서 지연 로딩한다.
  3. 가격·기업정보·실적일·QQQ·뉴스·애널리스트처럼 독립적인 요청은 제한된 병렬 처리로 묶는다.
  4. Conflict Board는 핵심 데이터가 준비되는 즉시 먼저 보여 주고, Peer·소유구조 상세는 뒤에서 채운다.
     정밀 검토 진입은 핵심 근거 수집이 끝날 때까지 비활성화한다.
  5. 단순한 "분석 보강 중" 문구 대신, 완료된 작업 수와 실제 측정된 요청 시간을 바탕으로 한
     **예상 잔여 시간**(예: "추가 근거 수집 중 · 약 7초 남음", "Peer 정보 8/12개")을 표시한다.
     고정된 가짜 카운트다운은 사용하지 않는다.

---

## 1. 프로젝트가 뭔지 (한 줄)

**"EnterTicker"** (구 "Devil's Advocate") — 나스닥 종목의 **매수/매도 관점을 한 번에
비교**해서 보여주는 웹앱. 이번 세션에 **전면 리브랜딩**이 있었음: Google 홈페이지 스타일의
검색엔진형 랜딩 화면 + "EnterTicker" 워드마크 아이덴티티로 전환. 기존 8단계 강제 흐름은
"정밀 검토"라는 선택 기능으로 남아있음(이전 세션에서 이미 재배치됨, 이번 세션은 리브랜딩+버그
수정+코드 정리 위주).

---

## 2. 파일 위치

| 내용 | 경로 |
|---|---|
| 로컬 프로젝트 루트 | `C:\Users\sangh\Desktop\Work\KoreaUniversity\devils advocate\` |
| 웹앱 코드 | `webapp/` 하위 (`app.py` 메인, `lib/` 모듈, `pages/` 서브페이지) |
| **GitHub 저장소** | https://github.com/zzang-pyeong/financial-assistant (public) |
| **배포된 앱** | https://financial-assistant-2g4f3appzsgmwj7aiwzbdzj.streamlit.app/ |
| Finnhub API 키 | 로컬: `webapp/.env` / 배포: Streamlit Cloud 대시보드 Secrets |
| 로컬 개발 서버 실행 설정 | `.claude/launch.json` (Claude Code 전용, `streamlit run webapp/app.py`) |
| Streamlit 설정 | `webapp/.streamlit/config.toml` (`showSidebarNavigation = false` — 이유는 6-A 참조) |

### pages/ 구성 (숫자 = 사이드바 순서)
1. `1_차트.py` (Price Chart) — 일봉/분봉 캔들차트
2. `2_섹터_Peer_비교.py` (Peer Compare), `3_Peer_목록.py` (Peer List)
3. `4_소유구조.py` (Ownership Map)
4. `5_애널리스트_뉴스.py` (Analyst News), `6_기업_이벤트_뉴스.py` (Company Events)
5. `7_옵션_데이터.py` (Options Data)

전부 영어 사이드바 라벨로 통일됨(이번 세션). 브라우저 탭 제목도 `"{Label} — EnterTicker"` 형식.

### lib/ 구성
- `data.py` — API 호출·캐싱 (yfinance, Finnhub, FinanceDataReader)
- `search.py` **(이번 세션 신규 생성)** — `fetch_and_store_ticker()`(티커 검색→전체 데이터
  수집→session_state 저장, 시작화면/사이드바 검색창 공유 로직), `render_sidebar()`/
  `render_sidebar_search()`/`render_sidebar_nav()`(사이드바 공용 컴포넌트)
- `peers.py` — Peer tier 분류 (이번 세션 전면 재작성, 아래 6-B 참조)
- `qualitative.py` — 뉴스 관련성/톤/이벤트 분류 (이번 세션 전면 재작성, 아래 6-B 참조)
- `page_helpers.py` — `inject_base_styles()`(폰트·스피너 CSS), `render_wordmark()`(브랜드
  타이포그래피), `require_analysis()`, `news_date_str()`
- `indicators.py`, `risk.py`, `ownership.py`, `translate.py`, `glossary.py`, `journal.py`,
  `config.py`, `charts.py` — 기존 유지

---

## 3. 핵심 설계 원칙

- **핵심 기능**: 티커만 입력하면 포지션 선택 없이 바로 **매수 관점 vs 매도 관점**("Conflict
  Board") 비교 화면이 뜬다. 사이드바 어디서나(모든 서브페이지 포함) 새 티커 재검색 가능.
- **원칙 B — 병치, 비집계** (유지): 지표를 하나의 점수로 합치지 않음.
- **"정밀 검토" (선택 기능)**: "🔍 정밀 검토 시작" 버튼을 눌러야 들어가는 기존 8단계 강제
  흐름(반대관점 강제확인 → 지지관점 → Conflict Board → 손절/익절 → 결정메모 → 최종확인 →
  기록). 이번 세션에 이 흐름의 헤더를 "Devil's Advocate" 구 브랜드에서 "🔍 정밀 검토 —
  EnterTicker"로 교체함(6-C 참조).
- **원칙 D — 정보 제공이지 자금관리 아님** (유지).
- **LLM/외부 API 키 추가 없음**: 뉴스 분류 개선을 Claude LLM으로 할지 논의했으나, 사용자가
  "돈 안 드는 방향"을 명시적으로 선택 → **규칙 기반(정규식 단어경계 매칭)으로만 구현**.
  `requirements.txt` 변경 없음, `anthropic` SDK 등 추가 안 됨.

---

## 4. 기술 스택 & 데이터 소스 (변경 없음, 이전 문서와 동일)

Python 3.11 + Streamlit(멀티페이지) / FinanceDataReader(일봉) / yfinance(펀더멘털·소유구조·
분봉·옵션·**산업/섹터/시총 메타데이터, 이번 세션 신규 활용**) / Finnhub(peer·뉴스·애널리스트) /
deep-translator / plotly / SEC EDGAR 미착수.

---

## 5. 사용자 흐름

```
[검색] EnterTicker 랜딩 화면 — Google 홈처럼 중앙 정렬 워드마크 + 검색창만
   (티커/기업명, 한글·영문 가능, Enter로 제출)
   ↓
[Conflict Board] 매수 관점 vs 매도 관점 병치. 사이드바에서 어디서든:
   - 새 티커 재검색 가능 (모든 페이지 공통, render_sidebar_search())
   - 차트·Peer 비교·Peer 목록·소유구조·애널리스트뉴스·기업이벤트뉴스·옵션데이터 이동 가능
   ↓ (선택) "🔍 정밀 검토 시작" 클릭 시에만 아래로 진입 (기존 8단계 강제 흐름, 로직 변경 없음)
1~8단계 (이전 인계 문서와 동일, 생략)
```

---

## 6. 이번 세션 작업 내역

### 6-A. UI 리브랜딩 (EnterTicker) — 커밋됨 (`7b8b728`)
- 랜딩 화면을 Google 홈페이지 스타일로 재구성: 중앙 정렬 "Enter**Ticker**" 워드마크(두 번째
  단어만 블루 강조), 좁은 레이아웃(`layout="centered"`, 검색 후엔 `"wide"`로 전환), 검색창+
  버튼을 하나의 둥근 필(pill)로 병합, Enter 키 제출 지원.
- 모든 서브페이지에 동일한 워드마크 스타일 적용(`render_wordmark()`), 사이드바/탭 제목 영어화.
- 전체 폰트를 IBM Plex Sans KR로 통일, 기본 스피너를 회전하는 🌐 이모지로 교체
  (`inject_base_styles()`, Streamlit 내부 아이콘 폰트를 건드리지 않도록 `stIconMaterial`은
  예외 처리 — 안 그러면 사이드바 화살표 아이콘이 깨짐, 실제로 한 번 겪었음).
- **사이드바 검색창을 모든 페이지에 추가**: `lib/search.py`의 `render_sidebar_search()` +
  `render_sidebar_nav()` → `render_sidebar()`로 통합. 처음엔 서브페이지에 네비 링크가 안 떠서
  ("Peer Compare 페이지에서 사이드바에 검색창만 있고 다른 페이지로 갈 링크가 없음") 버그
  리포트 받고 수정함 — `showSidebarNavigation=false`(Streamlit 기본 자동 네비 끔)로 설정해둔
  상태에서 커스텀 네비 링크를 app.py에만 넣었던 게 원인. `render_sidebar()`로 통합해서 8개
  파일(app.py+7페이지) 전부 동일한 사이드바가 뜨도록 고침. **실증 검증 완료.**
- Conflict Board 화면 이름 변경 이력: "매수관점 vs 매도관점" 텍스트 → "Conflict Point" →
  최종 "Conflict Board"(기존 정밀검토 흐름의 Step4와 이름 통일).
- 차트 페이지 분봉→일봉 전환 안 되던 버그 수정 (`pages/1_차트.py`): 분봉 라디오에
  `on_change=_clear_intraday_choice` 추가 — 이전엔 `index=None`이 최초 렌더에만 적용되고,
  한 번 분봉을 선택하면 되돌릴 방법이 없었음(라디오는 자체 상태를 유지하고 재선택 전엔 안
  풀림). **실증 검증 완료.**

### 6-B. Peer tier 분류 정상화 + 뉴스 규칙 고도화 — 커밋됨 (`7b8b728`)

**버그**: NVDA 검색 시 AMD가 "Tier1"(진짜 동종 peer)로 안 잡히던 문제. 원인은
`lib/peers.py`의 tier 판정이 "경쟁사 여부"가 아니라 **광물 니치 키워드(rare earth/lithium/
uranium/copper) 공유 여부**만 봤기 때문 — NVDA 사업요약엔 그 단어가 없어 전 peer가 강제로
Tier2가 됨.

**수정**: tier 판정을 **동일 산업(yfinance industry) → 동일 섹터+시총밴드(0.1~10×, sector
폴백 시에만) → 니치 키워드(오버라이드로 유지, USAR 같은 소형 광물주 대응)** 순으로 재구성.
`tier_basis` 필드 추가(표시용, additive). **실증 검증 완료** — NVDA 검색 시 AMD·INTC·QCOM·
AVGO·MU·TXN·ADI·MRVL 등 11개 peer가 전부 Tier1(동일 산업)로 표시됨. USAR 회귀 테스트도 통과
(니치 키워드 오버라이드 정상 작동).

**뉴스 결함 2가지**: (a) 관련성 필터가 아예 없어서 지나가는 언급·시장전반 기사도 종목에 귀속됨,
(b) `kw in headline` 부분문자열 매칭이라 "cut"이 exe**cut**ive에, "miss"가 co**mmiss**ion에,
"win"이 s**win**g에 오탐. "CEO"는 단순 언급만 해도 "경영진 교체"로 오분류.

**LLM 도입 여부 논의**: Claude API로 관련성+톤+이벤트를 한 번에 배치 분류하는 방안을 설계까지
마쳤으나(모델/프롬프트/캐싱 전략/폴백 로직 전부 계획서 작성함), **사용자가 최종적으로 "돈 안
드는 방향"을 선택** → 규칙 기반 고도화로 확정. LLM 관련 코드/의존성은 전혀 추가 안 됨.

**수정 (`lib/qualitative.py`, 순수 규칙 기반, 무료)**:
- 단어경계 정규식 매칭(`_matched_keywords`)으로 부분문자열 오탐 제거.
- 관련성 게이트(`is_relevant`) 신규 추가 — Finnhub `related` 필드 + 헤드라인/요약에 티커·
  기업명 단어경계 언급 여부로 판정. `classify_news_tone`/`filter_analyst_related_news`/
  `filter_corporate_event_news` 전부 `ticker`, `company_name` 파라미터 추가받아 무관 기사 드롭.
- "경영진 교체"는 직함(`ceo`/`cfo`/`chief executive`...) + 변경동사(`steps down`/`resigns`/
  `appoints`...)가 **둘 다** 있을 때만 매칭하도록 분리.
- `lib/search.py`에서 위 함수 호출 시 `ticker`/`company_name` 전달.
- **단위 테스트 + 브라우저 실증 완료** (오탐 케이스들 개별 확인, NVDA 뉴스가 실제 NVIDIA
  관련 기사로 정리됨 확인).

### 6-C. 코드 감사 및 정리 — **커밋됨** (`4e9fa67`) (사용자 요청: "코드 최적화, 안 쓰는 부분 찾아서
삭제, 에러날 곳 수정"). 전체 코드베이스 감사 후 아래를 적용했다. **브라우저 실증 검증은
세션 중단으로 아직 미완료**:

**죽은 코드 삭제** (감사 결과: 이 체인 외엔 unused import/function 전혀 없음, pyflakes로
`app.py`+`lib/*.py`+`pages/*.py` 전체 재확인 — 0 warning):
- `lib/data.py`: `get_institutional_holders()` 삭제 (호출부가 전부 죽어있었음)
- `lib/ownership.py`: `KNOWN_FIRM_TYPES`, `classify_firm_name()`, `get_firm_level_holders()`
  삭제 — `st.session_state.firm_holders`를 계산은 했지만 어느 페이지도 표시 안 하고 있었음
  (매 검색마다 낭비되는 yfinance 호출이었음).
- `lib/search.py`: 위에 맞춰 import·호출·session_state 업데이트에서 `firm_holders` 제거.
- `app.py`: `news_date_str()` 중복 정의 제거 — `lib/page_helpers.py`의 동일 함수를 import해서
  재사용하도록 변경. 그 여파로 이제 안 쓰는 `datetime` import도 제거.

**버그 수정** (`app.py`):
- **실적 D-day 로직 버그**: `days_left <= 10`이면 "임박"으로 표시했는데, 캐시된 실적일이 과거
  날짜면 `days_left`가 음수가 되어도 조건을 통과해서 "이미 지난 실적"을 "임박"이라고 잘못
  안내하던 버그. `0 <= days_left <= 10`으로 수정(두 곳: `render_neutral_context()`와 compare
  화면), 과거 날짜인 경우 별도 안내 문구 추가.
- **"Devil's Advocate" 잔재 브랜딩 제거**: 정밀 검토 흐름(Step 2~8) 헤더가 리브랜딩 이전 이름
  "📉 Devil's Advocate — 스윙 트레이딩 의사결정 보조"로 그대로 남아있던 걸 "🔍 정밀 검토 —
  EnterTicker"로 교체.

**`lib/search.py`에 추가된 방어 코드** (신규, 미검증):
- 가격 이력이 **60거래일 미만**인 종목(최근 상장 등)은 `compute_indicators()`에서 MA60/
  60일 전고점 등 롤링 지표가 `NaN`이 되고, 그 `NaN`이 그대로 Step5의
  `st.number_input(value=...)`까지 흘러들어가 크래시할 수 있는 구조였음(감사에서 발견,
  "PLAUSIBLE" 등급 — 실행으로 확인은 못 했으나 정적 추적상 타당). 데이터 수집 직후
  `len(df) < 60`이면 친절한 에러 메시지로 막고 중단하도록 가드 추가. **이 가드 자체의 동작은
  아직 브라우저로 확인 안 됨** — 다음 세션에서 신규 상장 종목(또는 짧은 기간 mock)으로 검증 필요.

**감사에서 발견했지만 손대지 않은 것들** (design smell로만 기록, 실제 크래시는 아님이라 판단):
- `lib/ownership.py`의 `get_fund_level_active_passive`/`get_recent_insider_transactions`가
  yfinance 스크래핑 컬럼(`Holder`, `pctHeld`, `Text`, `Shares`)을 방어 없이 직접 인덱싱함 —
  yfinance가 스키마를 바꾸면 `KeyError`가 나서 **해당 종목의 전체 분석이 막힘**(에러 하나가
  전체를 막는 구조). 지금은 문제없이 동작 중이라 "일어날 수 있는 시나리오에 대한 방어 코드
  추가하지 않기" 원칙에 따라 건드리지 않음 — 실제로 문제가 생기면 그때 고칠 것.
- QQQ 시장 국면 조회(`lib/search.py`의 `qqq_df`)도 같은 이유로 실패 시 전체 분석이 막히는
  구조지만, 이미 넓은 try/except로 잡히고 있고 QQQ는 매우 안정적인 데이터라 낮은 우선순위.

### 6-D. 미해결 — 전체화면 사이드바 중복 버그 (재현 실패, 원인 미확정)

**사용자 리포트**: "Peer Compare 페이지를 전체화면으로 전환하면 사이드바가 2개 뜬다"
(스크린샷 확인 — 검색창+네비 링크 세트가 좌측에 두 벌 나란히 보이고 그 옆에 정상 본문이 있는
형태).

**시도한 재현 방법과 결과** (전부 로컬 프리뷰 서버 `http://localhost:8501`에서):
1. Plotly 차트의 "Fullscreen" 툴바 버튼을 실제 클릭 이벤트로 눌러 확인 →
   `stFullScreenFrame`이 `position:fixed, 전체 뷰포트(1920×1080), 불투명 흰 배경(rgb(255,255,255)),
   z-index:1000050`으로 **정상 동작**. 사이드바 z-index(999991)보다 높아서 CSS 스펙상
   사이드바를 완전히 가려야 하고, 실제로 겹쳐 보이는 현상은 재현 안 됨.
2. 브라우저 뷰포트를 1920×1080으로 리사이즈 → 사이드바 DOM 요소는 여전히 1개만 존재
   (`document.querySelectorAll('[data-testid="stSidebar"]').length === 1`).
3. JS `.click()`(비신뢰 이벤트)과 `computer.left_click`(신뢰 이벤트) 둘 다 시도 — 신뢰
   이벤트 쪽은 좌표가 계속 빗나가서 안정적으로 재현하기 어려웠음. 스크린샷 캡처 기능이 이
   샌드박스 브라우저 환경에서 아예 안 됨("Browser pane이 표시되지 않아 렌더링 안 됨" 에러) —
   그래서 픽셀 단위로 직접 눈으로 확인할 방법이 없었고, DOM/computed style 트레이스로만
   간접 추론해야 했음.

**결론**: 이 로컬 환경에서는 재현 실패. 사용자가 보는 게 로컬 프리뷰인지 배포된 Streamlit
Cloud 버전인지 구분 안 됨(배포판은 Streamlit 버전/번들이 다를 수 있음 — 과거에도 "재배포 후
이전 모듈이 메모리에 남아 ImportError" 같은 캐시 이슈가 있었음, Reboot으로 해결했던 전례
있음). **다음 세션에서**: (a) 사용자에게 정확히 어떤 버튼/동작으로 전체화면 전환했는지,
로컬 프리뷰인지 배포 사이트인지 재확인, (b) 가능하면 실제 스크린샷을 공유받아 DOM 구조 대신
시각적으로 직접 비교. 방어적 CSS(예: 사이드바를 fullscreen 오버레이보다 항상 아래에 두는
`:has()` 셀렉터)를 추가할 수도 있지만, 확인 안 된 원인에 대한 코드 추가는 피하고 재현부터
우선할 것.

---

## 7. 알려진 한계 / 남은 리스크 (이전 세션에서 이어짐, 갱신)

- **매매일지 미영속화**: Streamlit Cloud 재배포 시 `trade_journal.csv` 초기화. 미해결.
- **Streamlit Cloud 캐시 버그**: 새 코드 push 후 이전 배포의 파이썬 모듈이 메모리에 남아
  `ImportError`가 뜨는 경우 있음 → "Reboot app"으로 해결. 이번 세션에도 로컬 프리뷰에서 비슷한
  증상(수정한 함수가 반영 안 됨)이 여러 번 있었고, 매번 서버 재시작 + `__pycache__` 삭제로
  해결함 — 앞으로도 코드 수정 후 반영이 안 되는 것 같으면 우선 재시작부터 시도할 것.
- **뉴스 분류는 여전히 규칙 기반 근사치**: 이번 세션에 크게 개선했지만(관련성 게이트, 단어경계
  매칭, 경영진교체 이중조건) LLM 수준의 정밀도는 아님. Finnhub가 한 기사에 여러 티커를
  `related`로 묶어둔 경우(예: AI 섹터 전체를 다루는 기사) 여전히 통과할 수 있음. 사용자가
  "돈 안 드는 방향"을 명시적으로 선택했으므로, 이 한계를 넘으려면 다시 유료 API(LLM) 논의가
  필요함 — 섣불리 재도입하지 말 것.
- **`get_fund_level_active_passive`/`get_recent_insider_transactions`의 컬럼 직접 접근**:
  6-C에서 언급한 대로 yfinance 스키마 변경 시 전체 분석이 막히는 구조. 지금은 정상 동작 중이라
  손 안 댐 — 실제 장애 발생 시 우선 대응 대상.
- **전체화면 사이드바 중복 버그**: 6-D 참조, 미해결·원인 미확정.

---

## 8. 다음에 할 만한 것

- **0번 항목부터**: 커밋된 6-C 변경사항의 브라우저 실증 검증
- 6-D 전체화면 버그 재현 재시도 (사용자에게 정확한 재현 절차 확인 후)
- MVP 출시 전 백테스트 게이트 — 미착수
- 매매일지 영속화 (Google Sheets 등) — 미착수
- SEC EDGAR 연동 — 미착수

---

## 9. 작업 스타일/규칙 (누적)

- **정직하게 기록하는 문화**: 틀린 것도 조용히 고치지 않고 "정정 기록"으로 남김. 이번 세션도
  전체화면 버그를 재현 못 했다는 사실을 그대로 기록함(억지로 원인을 지어내지 않음).
- **push는 매번 확인 후 진행**: 이 세션에서도 커밋 1회(`7b8b728`) 전 확인받고 진행함. 예외 없음.
- **실증 검증 선호**: 코드 수정 후 반드시 로컬 Streamlit 서버 띄워서 실제 브라우저로 확인.
  단, 이번 세션 마지막 작업(6-C)은 검증 도중 세션이 끊겨 **미완료 상태로 인계됨** — 이 점
  명확히 인지하고 다음 세션에서 이어서 검증할 것.
- **비용 발생 여부는 사용자가 명시적으로 결정**: LLM 도입처럼 비용이 발생하는 방향은 반드시
  먼저 물어보고, "돈 안 드는 방향"이라고 답하면 정말로 무료 방법만 쓸 것 (외부 API 키·SDK
  추가하지 않기).
- **애매한 설계 판단은 추측하지 말고 물어볼 것**: 이번 세션도 LLM 모델 선택(Haiku vs Sonnet vs
  Opus), 뉴스 개선 방향(LLM vs 규칙), peer tier 분류 기준을 전부 AskUserQuestion으로 확인받고
  진행함.
- 로컬 개발 서버는 `.claude/launch.json`에 `devils-advocate-webapp` 이름으로 등록되어 있어
  Claude Browser의 `preview_start`로 바로 띄울 수 있음. **코드 수정 후 반영이 안 되는 것
  같으면 `preview_stop` → `__pycache__` 삭제 → `preview_start`로 재시작할 것** (이번 세션에서
  여러 번 필요했던 패턴).
