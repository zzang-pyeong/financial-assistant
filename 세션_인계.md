# 세션 인계 문서 — Devil's Advocate / EnterTicker 프로젝트

> 새 대화(컨텍스트)로 넘어갈 때 이 문서 하나만 붙여넣으면 프로젝트 전체 맥락을 파악할 수
> 있도록 작성했습니다. 작성일: 2026-07-25 (직전 세션 기준 대폭 갱신 — 이전 인계 문서를 덮어씀).

---

## 0. 지금 당장 확인할 것

**커밋/푸시 전부 완료된 상태입니다.** `git status` clean, `origin/main` = `07b80b6`.
당장 처리해야 할 미완료 작업은 없습니다 — 아래 "8. 다음에 할 만한 것"이 우선순위 목록입니다.

```
07b80b6 Add SEC filing search to Relationship Map, with real contract context on hover
30999ed Merge remote changes (parallel Peer-page rename + chart fix)
2714548 Reorder Peer pages, clean up Ownership Map, tweak Options Data wording
```

**⚠️ Codex 공존 중**: 사용자가 이 프로젝트를 Codex로도 동시에 편집합니다. 낯선 diff를 발견하면
에러가 없는 한 건드리지 말 것(이미 여러 번 파일명 변경·버그수정이 독립적으로 겹쳤음 — git
merge로 무리 없이 합쳐짐). `git push` 전엔 항상 `git fetch` + `git log main..origin/main`으로
새 원격 커밋 여부 확인할 것.

---

## 1. 프로젝트가 뭔지 (한 줄)

**"EnterTicker"** — 나스닥 종목의 **매수/매도 관점을 한 번에 비교**해서 보여주는 웹앱.
검색하면 바로 Conflict Board(매수 vs 매도 병치)가 뜨고, 원하면 "정밀 검토"(기존 8단계
강제 흐름)로 들어갈 수 있음. 이번 세션 최대 작업은 **관계도(Relationship Map)** 신규
기능 — 뉴스 + SEC 공시 기반으로 대상 기업과 실제 사업관계가 있는 회사를 그래프로 보여줌.

---

## 2. 파일 위치

| 내용 | 경로 |
|---|---|
| 로컬 프로젝트 루트 | `C:\Users\sangh\Desktop\Work\KoreaUniversity\devils advocate\` |
| 웹앱 코드 | `webapp/` 하위 (`app.py` 메인, `lib/` 모듈, `pages/` 서브페이지) |
| GitHub 저장소 | https://github.com/zzang-pyeong/financial-assistant (public) |
| 배포된 앱 | https://financial-assistant-2g4f3appzsgmwj7aiwzbdzj.streamlit.app/ |
| Finnhub API 키 | 로컬: `webapp/.env` / 배포: Streamlit Cloud 대시보드 Secrets |
| 로컬 개발 서버 실행 설정 | `.claude/launch.json` (`streamlit run webapp/app.py`) |

### pages/ 구성 (8개, 숫자 = 사이드바 순서 — **단, 실제 순서는 파일명이 아니라
`lib/search.py::_SIDEBAR_PAGES` 리스트 순서가 결정함**, 3번 참조)

1. `1_차트.py` (Price Chart)
2. `2_Peer_목록.py` (Peer List) — 2026-07-25 순서 변경으로 Peer Compare보다 먼저 옴
3. `3_섹터_Peer_비교.py` (Peer Compare)
4. `4_소유구조.py` (Ownership Map)
5. `5_애널리스트_뉴스.py` (Analyst News)
6. `6_기업_이벤트_뉴스.py` (Company Events)
7. `7_옵션_데이터.py` (Options Data)
8. `8_관계도.py` (Relationship Map) — 2026-07-25 신규

### lib/ 구성 (신규만 표기, 나머지는 기존 유지)
- `known_companies.py` **(신규)** — 관계도용 나스닥/뉴욕 대형주 정적 목록(~100개, `STATIC_KNOWN_COMPANIES`)
- `sec_filings.py` **(신규)** — SEC EDGAR 공시 검색 + 문맥 추출(4번 참조)
- `qualitative.py` — 뉴스 톤 분류 + `match_counterparties()`(관계도 뉴스 매칭) 추가됨
- `charts.py` — `render_relationship_graph_figure()`(관계도 그래프) 추가됨
- `search.py` — `_remember_search()`(검색 이력), `_SIDEBAR_PAGES` 순서 관리

---

## 3. 핵심 설계 원칙 (유지)

- 원칙 B — 병치, 비집계: 점수 합산 없음. 관계도도 마찬가지 — "관계 강도" 점수화 안 함
- 원칙 D — 정보 제공이지 자금관리 아님
- LLM/유료 API 없음 — SEC EDGAR도 무료·키불필요라서 선택된 것(원칙 유지)
- **PRD는 as-built 방식** — "지금 실제 코드가 뭘 하는지"만 기록.
  옛 기획서(`PRD_스윙트레이딩_의사결정보조.md`)는 이번 세션에 삭제됨.
  (2026-07-27 후속: 단일 `PRD_EnterTicker.md`가 `docs/PRD.md`(제품 방향) +
  `docs/AS_BUILT_TECH_SPEC.md`(구현 명세) + `docs/CHANGELOG.md`(변경 이력)로 분리되고,
  원본은 삭제됨)

---

## 4. 이번 세션 최대 작업 — 관계도(Relationship Map) 기능

여러 단계에 걸쳐 만들어짐. 순서대로:

### 4.1 최초 구현 (뉴스 기반)
- `lib/qualitative.py::match_counterparties()` — corporate_events(M&A/신규계약 키워드
  통과 뉴스)에서 "이미 아는 회사"(peer + 세션 검색이력)가 헤드라인에 등장하면 엣지 생성
- `lib/charts.py::render_relationship_graph_figure()` — 허브(현재 티커) 중심 원형 배치,
  `math.cos/sin`만 사용(networkx 등 새 의존성 없음)
- 처음엔 `pages/6_기업_이벤트_뉴스.py`에 얹었다가, 사용자 요청으로 **`pages/8_관계도.py`
  전용 페이지로 분리**(`_SIDEBAR_PAGES`에 항목 추가)

### 4.2 유형·상태·근거 세분화
- "신규 계약/파트너십"을 공급계약/합작투자/라이선싱/전략적제휴로 세분화
- 진행상태: 철회·무산(점선 표시) / 발표·추진 / 체결·진행
- evidence_level 필드 도입(처음엔 하드코딩 버그 있었음 — Tesla 노드로 혼합 케이스 검증 후 수정)

### 4.3 커버리지 확장 — "너무 적다" 피드백
- **문제**: peer 리스트(경쟁사만)+세션 검색이력만으론 티커당 엣지 2~3개뿐
- **`lib/known_companies.py`** 신규 — 나스닥/뉴욕 대형주 ~100개 정적 목록 추가
- 티커 심볼 폴백 매칭(원문 그대로 대문자, 3자 미만 제외) 추가
- 뉴스 매칭용 불용어(`_NAME_SUFFIXES`)에 "semiconductor"/"technology" 등 업종 일반명사
  확장 — "ON Semiconductor"/"TE Connectivity"류 오탐 방지

### 4.4 SEC EDGAR 통합 (`lib/sec_filings.py`, 신규 파일)
- 사용자가 "공시자료도 써봐" 요청 → SEC Full-Text Search API(무료, 키 불필요) 통합
- **중요 발견**: 일반 관계 문구("supply agreement" 등) 검색은 거의 안 걸림(공시는 격식체) —
  대신 **상대 회사 이름 자체**를 대상 기업 CIK로 제한해서 검색
- known_companies 전체(peer+정적목록, 티커당 최대 ~110개)를 `ThreadPoolExecutor(max_workers=5)`
  로 병렬 검색, `pages/8_관계도.py` 방문 시 지연 로딩(진행률 바 표시)
- NVDA 실측: 뉴스만 2~3개 → 뉴스+공시 15개 회사/52개 엣지(CoreWeave/MSFT/Oracle 등 —
  peer도 뉴스도 아니었던 진짜 고객사 관계)

### 4.5 오탐 방지 캐스케이드 (실제 버그 발견 후 수정)
- "Arm Holdings plc"→"Arm", "Apple Inc."→"Apple"처럼 접미사 제거 후 흔한 영단어만 남는
  후보가 있음을 실측 확인(AMKR이라는 무관 회사 공시에 "Arm" 9건/"Apple" 5건 히트 —
  "at arm's length"/"apple-to-apples" 관용구 충돌로 추정)
- 해결: **정식 법인명 → 접미사 1단계 제거(안전한 별칭) → 완전 축약** 순서로 검색, 마지막
  단계까지 가서야 히트 나면 `evidence_level`에 "⚠️ 흔한 단어 검색이라 오탐 가능" 경고 추가
- 대부분 후보(~98개)는 여전히 1회 검색이라 성능 영향 거의 없음(위험 단어일 때만 캐스케이드)

### 4.6 hover에 실제 계약 문맥 표시 (최신 작업)
- **요구사항**: "노드에 커서 올리면 무슨 계약인지 보고 싶다" — 기존엔 "10-K에 'X' 언급"
  이라는 내용 없는 문구뿐이었음
- SEC 검색 API는 스니펫을 안 줌 → **공시 문서 원문을 직접 fetch해서 회사명 주변 텍스트를
  잘라내는 방식**으로 해결(`re`+`html` 표준 라이브러리만, 새 의존성 없음)
- `lib/sec_filings.py::attach_context_snippets()` — 회사당 최신 엣지 1건만 문서 fetch(대역폭
  절약), 상위 20개 회사까지만(그래프는 어차피 10개만 표시)
- 뉴스 쪽도 Finnhub `summary` 필드(이미 fetch하고 있었는데 안 쓰고 버려지던 데이터)를
  이제 사용 — 헤드라인보다 상세
- **실측 확인된 흥미로운 부수효과**: 스니펫을 열어보니 AMD/QCOM/AVGO/CSCO 등은 전부
  "경쟁사 목록"(위험요소 섹션) 문단이었고, MSFT/INTC/TXN/ORCL은 "신임 임원이 이 회사
  출신"이라는 인사 공시였음. ARM은 스니펫이 실제로 "at arm's length" 계약 조항임을
  보여줘 4.5의 오탐 경고가 정당했음이 재확인됨 — "언급됨"의 실체를 사용자가 직접 판단
  가능해짐(억지 분류 안 하는 게 오히려 정직함)

### 4.7 known 한계 (기록만, 미해결)
- **IREN이 안 뜸**: NVDA의 최근 4년 공시에도 "IREN" 없고 최근 뉴스에도 없음(뉴스는 매일
  바뀌므로 유동적). 추정 원인 — **지금 구조는 대상 티커(NVDA) 쪽 공시/뉴스에서만 상대
  회사 이름을 찾는 단방향 검색**. IREN처럼 작은 회사가 "우리가 NVIDIA와 계약했다"를
  자기 공시에 크게 실었을 가능성이 큰데, 그건 안 잡힘. **양방향 검색(상대방 공시도 검색)은
  사용자 확인 하에 이번엔 보류** — SEC 호출량이 약 2배로 늘어남. 다음에 논의 필요
- 공시 검색은 여전히 known_companies(peer+정적목록) 안의 후보만 인식 — open-world 발견 아님
- 관계 방향성(누가 인수/피인수, 공급/수요)은 여전히 모름
- User-Agent가 `"EnterTicker research contact@example.com"`(placeholder) — 실제 배포용으론
  진짜 연락처로 바꾸는 게 SEC 정책 취지에 더 맞음(기능은 지금도 정상 작동)

---

## 5. 이번 세션 그 외 작업

### 5.1 Peer 페이지 순서 변경
- `2_섹터_Peer_비교.py`→`3_섹터_Peer_비교.py`, `3_Peer_목록.py`→`2_Peer_목록.py` 파일명 교체
- **기술적으로 중요한 발견**: 사이드바 순서는 파일명 숫자가 아니라 `lib/search.py`의
  `_SIDEBAR_PAGES` 리스트 순서가 결정함(커스텀 사이드바라 Streamlit 자동 네비 무관) —
  파일명만 바꾸고 리스트를 안 바꾸면 순서가 안 바뀜, 둘 다 해야 함

### 5.2 Ownership Map 정리
- 요약표와 완전히 중복되던 raw-value 블록 삭제
- `insider_pct_interpretation()` 신규(구간별 해석: 30%+/10~30%/5~10%/5%미만)
- "🔽 지표별 구간 해석" 별도 아코디언으로 분리(값 표와 해석 텍스트 분리)
- 유동주식비율 결측 시 근사치 계산(총발행주식×(1-내부자보유율))으로 폴백 + "(추정치)" 표기

### 5.3 Options Data 페이지
- "만기 선택 → 행사가별 상세" → "Call/Put Options"로 부제목 변경
- 해석 expander에 volume(거래량) vs OI(미결제약정) 설명 추가
- 콜/풋 체인 정렬을 strike 오름차순 → OI 내림차순으로 변경

### 5.4 Peer 비교 지표 확장 — **미반영, 논의 중**
- `논의_Peer비교_지표확장.md` 작성 — 실제 5개 티커(NVDA/AMD/USAR/AMKR/IREN)로 후보 지표
  yfinance 채움률 실측 후 우선순위 제안(1순위: Current Ratio+Operating/Gross Margin,
  2순위: EV/EBITDA+매출성장률, 3순위는 기존 지표와 중복/소형주에서 약함)
- **`lib/peers.py`는 아직 안 건드림** — 우선순위 확인 대기 중

### 5.5 기타
- `fetch_and_store_ticker()`의 미사용 perf_counter 타이밍 계측 제거(Codex가 추가했다가
  UI에 표시 안 하고 방치된 코드였음)
- 로컬 환경 이슈 2건 해결: `websockets`/`uvicorn` 버전 충돌(→`pip install websockets==14.2`),
  SEC User-Agent 403 차단(curl로 검증한 정확한 문자열과 실제 코드 문자열이 달랐던 게 원인)

---

## 6. PRD·문서 현황

- **`docs/PRD.md`** — 제품 방향·목표·비목표 (2026-07-27 분리 신설)
- **`docs/AS_BUILT_TECH_SPEC.md`** — as-built 구현 명세 (파일·함수·매칭규칙·한계)
- **`docs/CHANGELOG.md`** — 날짜별 변경 이력
- ~~`PRD_EnterTicker.md`~~ — 위 3개 문서로 분리된 뒤 2026-07-27 삭제됨
- **`관계도_SEC공시_확장.md`** — SEC 공시 소스 확장 작업 요약(동기/아키텍처/실측결과/버그)
- **`논의_Peer비교_지표확장.md`** — Peer 지표 확장 논의 초안, 미구현
- **`relationship_map_design_handoff.md`** — 사용자가 작성한 "정공법" 설계안(SEC EDGAR/
  Finnhub Supply Chain API/LLM 추출 등). 지금 구현은 이것보다 스코프를 크게 좁힌 무료 버전
- **`부록_손절익절_계산스펙.md`**, **`데이터소스_조사_나스닥.md`** — 기존 참고 문서, 변경 없음

---

## 7. 알려진 한계 / 남은 리스크 (누적)

- **매매일지 미영속화**: Streamlit Cloud 재배포 시 `trade_journal.csv` 초기화. 미해결.
- **관계도 IREN 미표시**: 4.7 참조, 양방향 검색 필요하나 보류 중
- **관계도 User-Agent**: placeholder 이메일 사용 중(기능엔 문제 없음)
- **관계도 원문 링크 미노출**: hover에 스니펫은 뜨지만 실제 SEC 문서로 바로가는 클릭
  가능한 링크는 아직 UI에 없음(그래프 아래 표 형태로 만드는 방안 검토 가치 있음 —
  사용자가 공유한 리뷰 문서가 지적했던 사항)
- **관계도 KPI 미분리**: "상대기업 15개·뉴스근거 3건·SEC언급 52건" 식으로 소스별 개수를
  분리 표시하는 것도 리뷰 문서 제안 사항, 미반영
- **전체화면 사이드바 중복 버그**: 예전부터 이어진 미해결·원인 미확정 이슈(6-D, 오래된 기록)
- **면책 고지·데이터 신선도 표시**: 여전히 어디에도 없음(PRD 9번 표에 이미 기록된 공백)
- **Peer 비교 지표**: 확장 우선순위 확인 대기 중(5.4 참조)

---

## 8. 다음에 할 만한 것 (우선순위 순 아님, 상황에 맞게 선택)

1. `논의_Peer비교_지표확장.md` 우선순위 확인 후 `lib/peers.py` 반영
2. 관계도 양방향 검색 여부 논의(IREN류 케이스 해결)
3. 관계도 원문 링크를 그래프 아래 클릭 가능한 표로 노출
4. 관계도 KPI 분리 표시(뉴스/공시 엣지 수 구분)
5. SEC User-Agent를 실제 연락처로 교체
6. 백테스트 게이트, 매매일지 클라우드 영속화 — 미착수 상태 유지
7. 전체화면 사이드바 중복 버그 재현 재시도

---

## 9. 작업 스타일/규칙 (누적)

- **정직하게 기록하는 문화**: 틀린 것도 조용히 고치지 않고 정정 기록으로 남김. 이번
  세션도 evidence_level 하드코딩 버그, Arm/Apple 오탐을 숨기지 않고 그대로 기록함
- **push는 매번 확인 후, 항상 fetch 먼저**: Codex와 병행 작업 중이라 매 push 전
  `git fetch` + `git log main..origin/main`으로 새 원격 커밋 확인 필수
- **비용 발생 여부는 사용자가 명시적으로 결정**: SEC EDGAR도 "무료·키불필요"임을 먼저
  확인하고 진행함. 회사 웹사이트 스크래핑은 범용화 불가로 스코프 제외(사용자 확인)
- **애매한 설계 판단은 추측하지 말고 AskUserQuestion으로 확인**: 이번 세션도 데이터
  소스 범위, 검색 범위(peer만 vs 전체), 양방향 검색 여부를 전부 확인받고 진행함
- **큰 기능 추가 전엔 EnterPlanMode로 설계 먼저**: 관계도 기능 자체, SEC 통합, 오탐 캐스케이드,
  문맥 추출까지 전부 계획 승인 후 구현 — 실측 검증(curl/오프라인 스크립트)을 설계
  단계에서 먼저 하고 계획에 근거로 남기는 패턴이 자리잡음
- **실증 검증 필수**: 코드 수정 후 반드시 로컬 Streamlit 서버로 브라우저 검증. 이번
  세션도 컴파일/pyflakes → 오프라인 스크립트 검증 → 브라우저 검증 순서를 거의 매번 지킴
- 로컬 개발 서버는 `.claude/launch.json`의 `devils-advocate-webapp`으로 `preview_start`
  실행. **코드 수정이 반영 안 되는 것 같으면 `preview_stop` → `__pycache__` 삭제 →
  `preview_start` 재시작**(이번 세션에도 여러 번 필요했음)
- 브라우저 자동화 시 `computer.left_click` + `type`이 가끔 씹히는 경우가 있었음 —
  `form_input`으로 값을 직접 채우거나, JS로 폼의 실제 버튼을 찾아 `.click()`하는 방식이
  더 안정적이었음(이번 세션에서 반복적으로 필요했던 우회 패턴)
